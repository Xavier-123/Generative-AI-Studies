# 7.1 工作经历

## 7.1.1 **马上消费金融有限公司**（2025.10-至今）

### A. [对话数据标注自进化Agent系统](https://chatgpt.com/c/6a2a4aff-22ac-83ea-bdf4-3631c4dbf48d) - 智能体（⭐）

#### 简历版

```markdown
项目名称：对话数据标注自进化Agent系统
技术栈：Agent Framework, LLM, Prompt Optimization, Text Grad, GRPO, Dynamic RAG, LangGraph
项目描述：针对大模型在垂直业务落地中手工 Prompt 调试成本高、质量不稳定的痛点，设计并实现了一套参数无关（Parameter-Free）的推理增强系统，通过在标注过程中动态沉淀领域经验与自动优化 Prompt，实现低成本迭代。
核心工作：
	1.自进化标注与非参数 GRPO 优化:基于 Few-shot 构建分类体系，结合 GRPO 多类别对比机制强化分类边界学习，并引入 TextGrad 对推理轨迹进行分析生成修正规则，沉淀至经验库。在业务中将“电销黑名单”分类准确率从 81.9% → 89.96%，“试驾邀约”意图识别从 58% → 73%，平均提升 8%+。
	2.分层协同与动静隔离架构:构建“主分类 + 易错诊断 + 专有二分类”多 Agent 协同结构，将复杂迭代反思放入异步编译期执行；在线推理仅保留轻量 Prompt + 动态经验检索，实现小时级策略迭代与显著降低人工成本。
	3.防漂移评测与动态经验管理:引入 Golden Set 回归评测防止语义漂移，结合 BM25 + 向量检索的混合召回机制，并对经验库进行周期性去重与合并控制规模，保证系统稳定性与低延迟（单次推理延迟增量控制在秒级）。
```



```
                    [输入文本数据]
                          │
                          ▼
            ┌──────────────────────────┐
            │     1. 主分类 Agent       │◀── (检索宏观经验: Top-K)
            └─────────────┬────────────┘
                          │
         ┌────────────────┴─────────────────┐
         ▼                                  ▼
【命中易分类别 E, F】          【属于复杂/易混淆类别 A, B, C, D】
         │                                  │
         ▼                                  ▼
  [结束, 输出结果]              ┌──────────────────────────┐
         │                     │    2. 易错诊断 (离线)     │
   提炼宏观通用经验              │  统计混淆矩阵，生成决策树  │
                               └────────────┬─────────────┘
                                            │
                                            ▼
                                ┌──────────────────────────┐
                                │ 3. 专有二分类 Agent (第一轮)│◀── (检索 A vs [B,C] 微观经验)
                                │    判断: 是 A 还是 [B,C]? │
                                └───────────┬──────────────┘
                                            │
                               ┌────────────┴────────────┐
                               ▼                         ▼
                           【判定为 A】              【判定为 [B,C]】
                               │                         │
                        [结束, 输出结果 A]               ▼
                                            ┌──────────────────────────┐
                                            │ 专有二分类 Agent (第二轮)  │◀── (检索 B vs C 微观经验)
                                            │     判断: 是 B 还是 C?    │
                                            └───────────┬──────────────┘
                                                        │
                                                        ▼
                                                  [结束, 输出结果]
```



这个多 Agent 协同结构的设计非常巧妙，它本质上是用**“分治法（Divide and Conquer）”**和**“编译期/运行期分离（动静隔离）”**的思想，解决了大模型在处理复杂多分类（特别是有交集、易混淆类别）时**Prompt 过长、注意力分散、边界模糊**的痛点。



“主分类 + 易错诊断 + 专有二分类”思想：构建金标数据集（尽量覆盖当前阶段的真实场景），然后先用主分类跑一遍数据，可以总体判断哪些类别准确率和召回率高，标为“易分类别”，然后再重新进入自进化agent系统优化，第一步进入主分类，去经验库中获取topk的分类经验动态组装提示词，如果分为“易分类别”，则该条数据的流程结束，并且使用非参数GRPO算法从多个类别提取分类经验；第二步，统计分析所有判定为其它类别的数据，看看哪些类别容易混淆（比如类别A有很大比例的数据都误判为B，则认为A，B两类别容易混淆），制作新的两类数据集；第三步，将所有两类数据集，传入专有二分类，通过非参数GRPO优化具有针对性的提取两个类别间的经验，并保存到经验库。

具体例子为：假设有A、B、C、D、E、F六个类别的数据，EF被认为易分类别，则经过主分类后，我们需要统计分析ABCD这四个类别数据，分析出 A、B、C 这三个类别易混淆，即A容易判断为B，A也容易判定为C，那么在设计专有二分类的时候，我们可以将B和C合为一个新的类别，A和其它，经过第一个专有二分类之后如果判定为A则结束，如果认定为其它，则再经过一次专有二分类用于学习判定B和C的边界条件。

主分类提取经验可以理解为从宏观提取经验，专有二分类理解为微观提取经验

帮我理解这个多 Agent 协同结构



#### 模拟面试

```markdown
https://chatgpt.com/c/6a2d55bd-b3a8-83ea-9608-e0771a9229e3
https://chatgpt.com/c/6a2a4aff-22ac-83ea-bdf4-3631c4dbf48d

项目名称：对话数据标注自进化Agent系统
**项目角色**：独立设计与研发负责人
**项目背景**：针对大模型在垂直业务（如意图识别、复杂多分类、逻辑推理）落地中面临的**手工 Prompt 调试成本高、质量不稳定、领域知识难以持续积累**等痛点，设计并实现了一套无需参数微调（Parameter-Free）的推理增强系统。该系统通过在推理/标注过程中动态沉淀领域经验、自动优化 Prompt，实现了低成本、可解释的性能迭代。
**技术栈**：Agent Framework, LLM API, Prompt Optimization, Text Grad (文本梯度), GRPO, Dynamic RAG, Workflow Orchestration

**核心职责与技术实现**
1. **自进化语料标注机制**
   - 设计并实现自进化语料标注 Agent，利用少量种子样本（Few-shot Seeds）引导大模型自顶向下构建分类知识体系。
   - 建立多维推理链路分析模块，实现对海量对话数据的自动标注与持续优化，解决传统人工标注成本高、迭代慢的问题。
2. **非参数化 Multi-class GRPO 与 Text Grad 协同优化**
   - **范式扩展**：将 GRPO（群体相对策略优化）的“单样本-多轨迹”对比机制适配至提示词优化场景。通过在单个 Batch 中引入多类别样本，引导模型对跨类别的推理轨迹进行对比“多样本-多轨迹”，显式提炼分类边界。
   - **文本梯度反思**：基于 Text Grad 思想，通过 Agent 自省分析正逆向推理轨迹的差异，生成文本级“梯度修正意见”，并将提炼出的泛化规则以结构化 JSON 格式沉淀至动态经验库。
   - **自适应反馈调节**：设计与 Batch 准确率挂钩的自适应奖励函数。在低准确率区间降低更新门槛（侧重探索与快速修正），在高准确率区间收紧更新门槛（侧重利用与系统稳定），平滑引导 Prompt 与经验库的协同演进。
3. **分层 Agent 协同架构与动静隔离优化**
   - 构建由“多分类主 Agent + 易错类别二分类诊断 Agent”组成的分层协同闭环。
   - **运行期与编译期解耦**：将重资源的 Multi-class GRPO 迭代、Text Grad 反思等步骤放在异步“编译期”运行；线上真实推理（运行期）则采用“精简版 Prompt + 动态检索关联经验”的轻量化架构，降低系统延迟与 Token 消耗。
4. **量化评测门禁系统与防漂移机制**
   - **黄金验证集（Golden Set）防漂移门禁**：为防范自进化过程中的“语义漂移”与“灾难性遗忘”，建立自动化评测 Pipeline。每次 Prompt 或经验库迭代更新前，强制在黄金验证集上进行回归测试，指标未发生退化方可准入上线。
   - **经验库动态剪枝与路由**：引入基于向量与 BM25 的混合检索机制，在线推理时仅按需召回 Top-K 相关规则；同时设计定期合并与去重机制，设置近一段时间被检索测次数，为0则人为删除，保持经验库的规模处于合理区间。

**效果与业绩：**

- **多场景准确率显著提升**：方案在多个实际业务场景中完成落地验证。其中，“电销黑名单”分类准确率由 **81.9% 提升至 89.96%**，“试驾邀约”意图识别准确率由 **58% 提升至 73%**；多数主流场景的标注准确率平均提升 **8% 以上**，标注一致性与标准化程度得到明显改善。
- **具备鲁棒的自诊断与偏差发现能力**：在“质检分类”等未见明显指标提升的复杂场景中，利用系统的“易错类别发现 Agent”与自评估机制，主动诊断并定位出评估数据源自身存在的标注偏差，避免了算法盲目拟合噪声，验证了系统评估体系的客观性与严谨性。
- **标注效率跃升与人工降本**：通过全自动化的 Prompt 与经验库迭代流程，将策略调优周期由传统人工的**数天缩短至小时级**；大幅减少了标注过程中的人工干预和校验频次，显著提升了海量数据的整体标注吞吐量。
- **高工程性能与泛化扩展性**：通过引入动态经验库压缩机制，单次推理延迟增量控制在极低水平（毫秒级），满足高并发标注的性能要求；系统分层架构具备良好扩展性，可无缝复用至 RAG、Agent 等更复杂的上下文学习（In-Context Learning）场景。
```



**面试时的口语化讲法：**

我来介绍一个我独立牵头负责的核心项目：对话数据标注自进化 Agent 系统。

先说说背景，当时我们用大模型落地电销、邀约这类垂直业务的意图识别、复杂分类需求时，遇到了很实际的痛点：全靠人工调 Prompt，不仅调试成本高、效果不稳定，而且攒下来的领域经验也没法系统化沉淀、持续复用。所以我主导设计并落地了这么一套系统 —— 不用微调模型参数，纯靠推理侧的增强方案，让系统在标注和推理过程中自动沉淀领域经验、自主优化 Prompt，实现低成本、可解释的效果持续迭代。

具体我核心做了四块工作：

第一块是搭建了自进化的语料标注机制。不用一开始就投入大量人力做标注打底，只要给少量种子样本，就能引导大模型自顶向下搭建起完整的分类知识体系，自动完成海量对话数据的标注，还能边跑边优化，从根源上解决了传统人工标注成本高、迭代周期长的问题。

第二块是技术上最核心的设计：把 GRPO 群体相对策略优化和 Text Grad 文本梯度结合，做了一套适配多分类场景的非参数化协同优化方案。原本的 GRPO 是单样本对比多条推理轨迹，我把它扩展成了 “多样本 - 多轨迹” 模式，让模型在一个批次里同时对比不同类别的推理链路，主动提炼出清晰的分类边界；再基于 Text Grad 的思路，让 Agent 自己复盘做对和做错的推理差异，生成文本形式的修正意见，再把提炼出的通用规则结构化沉淀到动态经验库里。另外我还设计了和批次准确率挂钩的自适应奖励函数：准确率低的时候放宽更新门槛，优先快速纠错探索；准确率高了就收紧门槛，优先保证系统稳定，让 Prompt 和经验库能平滑协同演进。

第三块是设计了分层 Agent 协同架构，做了动静隔离的工程优化。我把系统拆成了多分类主 Agent + 易错类别二分类诊断 Agent，形成分层纠错的闭环。同时我把费算力的 GRPO 迭代、文本梯度反思这些重操作，全部放到后台异步的 “编译期” 运行；线上真实推理的 “运行期” 只用精简版 Prompt 加动态检索关联经验的轻量化方案，既保留了完整的自进化能力，又把线上的延迟和 Token 消耗压到了最低。

第四块是搭了完整的量化评测门禁和防漂移机制。自进化系统最怕越迭代越跑偏，也就是常说的语义漂移、灾难性遗忘这类问题。所以我做了黄金验证集门禁：每次 Prompt 或经验库要更新，必须先在固定的验证集上跑回归测试，指标不退化才能上线。同时针对经验库，我做了向量加 BM25 的混合检索，线上推理只召回最相关的 Top-K 规则；还加了定期合并去重、清理长期未被调用规则的机制，让经验库规模一直保持在合理区间，不会越用越臃肿。

最后落地效果也很扎实：

指标上，电销黑名单分类准确率从 81.9% 提升到了 89.96%，试驾邀约意图识别从 58% 涨到了 73%，大部分业务场景的标注准确率平均提升 8% 以上，标注一致性也提升了很多。

还有个挺有价值的点：有个质检分类的复杂场景，一直没看到明显指标提升，系统的易错诊断模块主动定位出 —— 是评估用的数据集本身就存在标注偏差，避免了我们盲目拟合噪声，也验证了这套评估体系的客观性。

效率上，原来人工调一轮策略要数天，现在系统自动迭代，小时级就能完成一轮，大幅减少了人工干预，标注吞吐量提升很明显。

性能上也控制得很好，线上推理的延迟增量只有毫秒级，完全扛得住高并发标注；而且这套架构扩展性很强，后面复用到 RAG、Agent 这类上下文学习场景也完全通用。



#### 模拟问题

##### Q1：为什么选择"无训练"方案而不是直接微调模型？

> **回答要点**：微调成本高（算力、数据标注）、周期长、难以快速迭代；无训练方案通过推理阶段积累经验，零算力成本、实时可更新，适合资源受限的业务场景。可对比 RAG（检索外部知识）与本方案（自生成内部经验）的差异。
>
> 原因1：场景变化太快(SFT 周期非常长)
> 原因2：数据规模不足(SFT 500~3000条标注数据)
> 原因3：解释性要求高(SFT 难解释)

##### Q2：为什么选择 TextGrad 框架？它们的核心差异是什么？

> **回答要点**：TextGrad 将"文本梯度"概念引入优化，适合需要多跳推理的复杂任务。

##### Q3：经验库如何避免"噪声经验"污染后续推理？

> **回答要点**：通过两阶段机制：① 单 Batch 内先生成 candidate_experiences 而非直接更新，避免单条坏样本直接写入；② Epoch 结束后的压缩阶段通过 LLM 二次审查合并冗余、淘汰低质量条目。可进一步讨论设置置信度阈值等改进方向。

##### Q4：你这个多Agent协同架构是什么？（+1）

> 

##### Q5：经验的"增删改合并"操作由 LLM 自身决策，如何保证 JSON 格式输出的鲁棒性？

> **回答要点**：代码中使用 `split('```json')[-1].split('```')[0]` 做格式抽取，外层 try/except 兜底，失败时跳过而非崩溃（continue）。可进一步讨论引入 Pydantic 校验、Few-shot 格式示例强化输出稳定性

##### Q7：如何评估先验经验的质量与有效性？

> **回答要点**：当前方案以最终答案正确率作为隐式反馈；可扩展讨论：引入显式的经验有效性评分（如某条经验在多少问题上有帮助）、A/B 测试（加/不加经验的准确率对比）。
>
> **双路消融对比**：
>
> - **方法**：在同一验证集（Validation Set）上运行两路 Pipeline：
>
>   - **对照组（Base Line）**：仅使用原始 Prompt，不引入 JSON 经验库。
>   - **实验组（Exp Line）**：在输入端注入当前版本的 JSON 经验库。
>
> - **评估指标**：计算 **准确率 ** 和 **F1-Score** 增量
>
>   。若某条经验加入后 ΔAcc < 0，说明该经验存在负迁移，应予以剔除或降级。
>
> **留一交叉验证**：
>
> 1. **留一交叉验证（Leave-One-Out Validation）**：
>    - **方法**：针对经验库中的多条经验 E={e1,e2,...en}E={e1,e2,...en}，每次有意识地移除其中一条 ei，观察整体表现的变化。
>    - **评估指标**：计算经验的**边际贡献度**。边际贡献为负或接近于零的经验会被标记为“低效经验”，触发清理机制。

##### Q8：新沉淀的经验与既有经验产生逻辑冲突，如何解决？（+1）

> 1. **语义冲突检测（Conflict Detection）**：
>    - **方法**：利用专有的“审计 Agent”或符号化规则，对新生成的 JSON 经验与既有经验进行两两比对。
>    - **评估案例**：若经验 A 指出“‘退款’关键词属于‘售后服务’”，而新经验 B 指出“‘退款’且伴随抱怨语时应归入‘投诉’”，系统需评估两者的覆盖边界。
>    - **解决机制**：若判定为冲突，则触发“规则融合（Merge）”或“条件细化（Refinement）”，通过补充前置条件（如 Context-specific constraints）来消解冲突。
> 2. **语义冗余度与合并（Redundancy & Deduplication）**：
>    - **方法**：计算新经验与旧经验在向量空间（Embedding）中的余弦相似度。
>    - **评估指标**：当相似度超过设定阈值（如 0.85）时，判定为表述冗余。系统将自动调用大模型进行“规则归并”，用更具泛化性的表述合并这两条经验。

##### Q9：如何提升跨样本的泛化能力？

> **回答要点**：高温度增加输出多样性，保证 N 条采样轨迹覆盖不同推理路径（正确与错误），为经验提取提供更丰富的对比素材。若 temperature 过低，N 条输出高度相似，经验提取价值有限。
>
> 1. **跨批次泛化测试**：
>    - **方法**：将在批次 T 中提炼出的经验，立即在完全独立的批次 T+1、T+2 上进行盲测。
>    - **评估指标**：**泛化衰减率。如果在提炼批次上准确率提升 15%，但在新批次上仅提升 1%，说明该经验过拟合了特定批次的语料（如特定客户的说话口癖），属于低质量经验。
> 2. **生存周期与置信度衰减**：
>    - **方法**：给每条 JSON 经验赋予一个初始置信度（Confidence Score）和调用计数器。
>    - **评估机制**：在后续标注中，每当某条经验被成功检索并调用：
>      - 若导致标注正确：置信度增加（奖励）；
>      - 若导致标注错误：置信度大幅扣减（惩罚）。
>      - 当置信度低于阈值，或者长期未被调用，该经验将被自动物理删除或归档，以保持经验库的“代谢活性”。
> 3. **引入“黄金验证集”的门禁机制**：
>    - **方法**：在每次经验库或 Prompt 更新后，不能仅依赖当前 Batch 的准确率，还需自动在代表性历史样本集上进行微调级别的回归测试。

##### Q10：`temperature=1.0` 的设计意图是什么？

> **回答要点**：高温度增加输出多样性，保证 N 条采样轨迹覆盖不同推理路径（正确与错误），为经验提取提供更丰富的对比素材。若 temperature 过低，N 条输出高度相似，经验提取价值有限。

##### Q11：这套系统如何从实验原型落地到生产环境？你会做哪些工程化改造？

> **回答要点**：① 经验库改为向量数据库（如 Chroma，代码中已有依赖）实现语义检索而非全量注入；② 增加经验版本管理与回滚机制；③ 异步任务队列处理大批量数据；④ 监控 API 调用成本与经验库增长曲线。

##### Q12：如果业务方反馈模型在新数据上效果下降如何排查？

> **回答要点**：① 检查经验库是否存在过拟合训练分布的噪声经验；② 对比加/不加经验的推理结果，定位经验质量问题；③ 检查数据分布漂移（新数据与训练数据的 prompt 模式差异）；④ 必要时清空经验库重新从新数据中提取。



##### Q11：先介绍一下这个项目。不要讲技术细节，重点讲业务背景、为什么要做、最终解决了什么问题。

```markdown

```

##### Q12：一个形象比喻

```markdown
形象比喻：医院诊疗系统
1.主分类 Agent（门诊预 triage 医生）
	职责：快速看一眼患者症状（文本数据）。
	逻辑：如果病情非常典型（比如普通感冒 E、皮疹 F，即“易分类别”），直接开药看好出院（完成标注），并记录经验；如果是疑难杂症（A、B、C、D），不强行下诊断，而是转诊。
2.易错诊断 Agent（专家会诊/离线病例分析）
	职责：不直接在病人面前干活，而是在“后台（编译期/离线）”统计所有疑难杂症的误诊记录（混淆矩阵）。
	逻辑：发现“医生经常把病症 A 误诊为 B 或 C”，于是下达指令：“针对 A、B、C，我们需要制定一套专属的极细化排除标准！”
3.专有二分类 Agent（专科专家/排除法诊断）
	职责：拿着极细化的排除指南，专门处理易混淆的这几个特定类别。
	逻辑：采用“排除法/二叉树”路线——先问“这是病情 A 还是 其他（B/C）？”；如果是其他，再问“这是 B 还是 C？”。一步步精准收敛。
```

##### Q13：你提到把GRPO改造成Prompt优化。详细讲一下。

```markdown
传统GRPO：优化对象是模型参数。
Multi-Class GRPO：
    把优化对象从：Model Weight
    变成：Prompt + 经验库规则

下一轮推理时：
	动态召回。效果逐步提升。
```

##### Q14：Multi-Class GRPO和普通GRPO区别是什么？

```markdown
普通GRPO：
    同一问题
    多个轨迹比较
    
Multi-Class GRPO：
	每个batch中包含多个类别
	跨类别边界比较
	
Hard Negative → 发现问题
TextGrad → 总结规律
经验库 → 长期记忆
```

##### Q15：为什么不在线学习？

```markdown
1.稳定性不可控。
2.GRPO+TextGrad 一次优化需要几十到上百次LLM调用，线上无法接受。
```

##### Q16：经验库越来越大怎么办？（+1）

```markdown
第一层：Embedding召回，经验向量化，TopK=10
第二层：BM25召回，关键词补充，防止向量漏召回
第三层：规则聚合，规则合并
第四层：检索次数少的去掉，且创建时间较早，去除老旧经验
```

##### Q17：线上出现过最严重的问题是什么？

```markdown
某批数据本身标注错误。
系统把错误样本当真理学习。
生成大量错误规则。
```

##### Q18：GRPO的奖励函数如何设计的？

```markdown
第一步：考核候选提示词的“三个指标”
	答得对不对（正确率得分）
	答得清不清晰（边界区分得分），这里不需要
	是不是太啰唆（字数冗余扣分），这里不需要

第二步：同台竞技，只看“相对优势”（GRPO 核心）
	每次迭代，系统会同时生成 5∼10 个不同的提示词版本。
    让这些版本在同一批测试数据上“同台竞技”，算出各自的总分（分析轨迹 + 最终答案）。
    计算它们的平均分。
    比平均分高得多的，定义为**“尖子生（正向反馈）”；比平均分低得多的，定义为“不及格生（逆向反馈）”**。
    文本梯度系统只会参考“尖子生”和“不及格生”的答题差异来进行针对性反思。
    
第三步：自适应调整考核重点（动态风控）
	模式 A：低分低谷期（系统还很笨的时候，比如正确率低于 60%）
    	考核重点：只看正确率。只要能答对，逻辑粗糙点、提示词长一点都没关系。
    	更新门槛：降到最低。只要有稍微好一点的修改，立刻通过，鼓励系统大胆探索。
    模式 B：高分平稳期（系统已经很聪明的时候，比如正确率高于 85%）
    	考核重点：转向边界清晰度和字数精简度。正确率高已经不稀奇了，我们要看谁的推理逻辑最干净、字数最少。
    	更新门槛：提到最高。只有能带来显著突破且不啰唆的修改才允许上线，防止系统“瞎折腾”导致线上不稳定。
```



##### Q20： 动态经验库的“规则冲突”与一致性如何维护？（+1）

1. **编译期冲突检测与消解机制（Consistency Checking）**：
   - 当有新规则拟写入经验库时，审计 Agent 负责检索库中语义相似的已有规则，并利用大模型进行一致性推理。
   - 若发现冲突，进行**“规则合并（Consolidation）”**，融合成一条描述更精确、边界更清晰的上位规则，或者通过显式声明优先级（e.g., if A then X, unless B）来解决。
2. **规则的时效性衰减（Time-decay / Usage-decay）**：
   - 为每条规则维护一个使用计数和准确率反馈指标。如果某条规则长期未被检索，或者被检索后导致线上推理判断失误，则降低其置信度权重，直至触发动态剪枝。



##### Q21：训练数据集如何更新的？（+1）



------

### B. 智能交互式信贷双录质检与主动反欺诈系统 - 后训练,多模态,智能体

#### 简历版

```markdown
# 项目名称：
智能交互式信贷双录质检与主动反欺诈多模态 Agent 系统
# 角色：
资深多模态大模型算法工程师
# 技术栈： 
Python, Function Calling, Multi-Agent Debate, WebRTC 实时流, 活体检测 SDK, ASR/TTS 引擎, Redis, vLLM
# 项目描述：
	针对消费金融远程双录面签流程，将训练完成的音画多模态大模型落地为生产级实时交互 Agent，实现审批过程中“边看视频边质检、主动多轮博弈问询、毫秒级调用风控工具”的全自动闭环。
# 核心工作：
1.主动博弈状态机与多模态意图规划：
	设计动态质检 Agent 决策流。若检测到用户眼神闪烁、证件反光或环境异常，Agent 自主规划打断并生成追问动作（如“请向右微转头部并读出随机数字”），实现对抗式身份核验。
2.Function Calling 级联风控工具链：
	集成静默活体检测 SDK、公安权威人脸比对接口、反欺诈设备指纹库与 ASR/TTS 模块，实现毫秒级交叉验证与工具调用。
3.Multi-Agent Debate 双向辩论审核：
	针对边缘高风险案例，引入“初审风控 Agent”与“质检反思 Agent”进行多轮证据链博弈，自动生成包含关键帧定格（Visual Evidence）的可解释性合规报告。
4.业务成果：
	支撑日均千万级远程信贷双录业务，端到端交互决策时延控制在 400ms 以内；全流程自动化通过率达 75%，人工复审率削减 65%，累计拦截数千万元欺诈风险。
```





#### 模拟面试



#### 模拟问题

##### Q1：阶段一和阶段二在工程上是怎么训的？

```markdown

```



### C. 金融风控智能 Agent 强化学习决策迭代系统 - 后训练,智能体（⭐）

#### 简历版

```markdown
# 项目名称：
金融风控智能 Agent 强化学习（RL）决策迭代系统
# 角色：
大模型后训练算法核心成员
# 技术栈：
PyTorch, DeepSpeed, Megatron-LM, vLLM, OpenRLHF, Qwen3.5-35B-A3B, GRPO/PPO, ReAct, Agent-DPO, Ray
# 项目描述：
	针对金融研报深度解析、复杂数值计算与多工具调用场景下存在的“数值幻觉严重、多步逻辑推导脆弱、工具调用格式不稳”等痛点，负责基于 Qwen3.5-35B-A3B 打造金融垂直领域大模型。主导搭建从 “领域知识注入 SFT → Agent 交互轨迹精调 → 规则驱动推理强化学习 (Reasoning RL) → 偏好与合规对齐 (DPO/PPO)” 的工业级全流程后训练体系。
# 核心工作：
阶段一：金融领域知识注入与长链推理 SFT (Domain & CoT SFT)
	数据工程与清洗：构建并清洗 15 万+ 金融高质量指令集（涵盖财报结构化提取、合规审计、估值建模及研报撰写等文本任务），制定基于多规则过滤与语义去重的清洗流水线。
	显式 CoT 思维链注入：针对财务指标计算（如 EBITDA、WACC、PE/PB）等数值密集任务，构建显式多步推理思维链（Step-by-Step CoT），规范推理步骤与符号定义，筑牢基座逻辑推理底座。

阶段二：Agent 工具调用与多轮决策精调 (Agent Multi-turn SFT)
	沙盒拒绝采样轨迹合成 (Trajectory Synthesis)：搭建基于真实金融 API（行情查询、研报库检索、SQL 执行器）的自动化沙盒生成管线。结合 Teacher Model 与环境真实执行结果，通过拒绝采样 (Rejection Sampling) 过滤并构建 5 万+ 包含“思考-调用-观测-自我修正”的黄金交互轨迹。
	精准 Loss Masking 机制：在多轮 ReAct/Function Calling 数据训练中，设计细粒度 Mask 策略，仅对模型自主生成的 Thought、Action (JSON) 及 Final Answer 计算梯度，严格 Mask 外部环境返回的 Observation，彻底消除环境噪声污染，大幅提升 JSON 格式遵从度。

阶段三：强化学习推理对齐与偏好优化 (Reasoning RL & Factuality Alignment)
	基于规则奖励的金融推理强化学习 (Rule-based RL via GRPO)：
		Reward 函数设计：针对金融计算与逻辑推理（FinQA），摒弃不可靠的神经奖励模型，采用纯规则驱动的复合奖励系统：包含数值精确匹配 Reward（结合 SymPy/正则解析）、推理过程格式规范 Reward（CoT 标签闭合）及冗余步骤惩罚。
		GRPO 算法落地：采用 GRPO (Group Relative Policy Optimization) 算法，利用组内相对优势估计替代 Critic 网络，在大幅节省显存的同时，显著激发模型在复杂金融数学计算与逻辑推导上的“顿悟 (Aha-moment)”与长链推理深度。
	事实性与合规偏好对齐 (DPO / RM-PPO)：
		构建 2 万+ 维度的 Pair 偏好对（正样本为事实准确、合规严谨的回答；负样本包含指标计算幻觉、违规投资建议等）。
		采用 DPO (Direct Preference Optimization) 进行多目标偏好对齐，有效抑制模型在非确定性金融事实上的“胡说八道”，使输出风格符合金融机构合规与研报撰写规范
```



```
项目名称：金融高阶智能体后训练与强化学习对齐系统
核心角色：大模型后训练算法成员 | 技术栈：Qwen3.5-35B-A3B, PyTorch, DeepSpeed, OpenRLHF, vLLM, GRPO, DPO, ReAct, Ray
项目描述：
针对金融研报解析、复杂计算与工具调用场景下的“数值幻觉、长链推导脆弱、格式不稳”等痛点，基于 Qwen3.5-35B-A3B 搭建从 “领域SFT → Agent轨迹精调 → 规则推理强化(RL) → 偏好合规对齐(DPO)” 的工业级后训练体系。
核心工作：
金融知识注入与长链推理 SFT：清洗构建 15万+ 高质量金融指令数据（涵盖财报提取、合规审计与估值建模）；针对 EBITDA/WACC 等指标计算引入显式多步思维链（Step-by-Step CoT），夯实数值推理底座。
Agent 轨迹合成与细粒度精调：搭建金融 API（行情/研报/SQL）沙盒环境，通过拒绝采样（Rejection Sampling）合成 5万+ 包含自我修正的黄金交互轨迹；设计细粒度 Loss Masking 机制（仅对 Thought/Action/Answer 计算梯度，Mask 环境回传），彻底消除噪声，显著提升 JSON/ReAct 调用遵从度。
规则驱动的推理强化学习 (GRPO)：针对 FinQA 场景摒弃神经 RM，采用纯规则驱动的复合 Reward（SymPy/正则数值精确匹配 + 标签闭合 + 冗余惩罚）；落地 GRPO 算法免去 Critic 网络节省显存，成功激发模型在复杂金融数学计算中的“顿悟”与深度长链推理能力。
事实性与合规偏好对齐 (DPO)：构建 2万+ 事实准确与合规维度 Pair 对，利用 DPO 进行多目标偏好优化，有效抑制指标幻觉与违规输出，使模型完全对齐金融机构合规及研报规范。
```



#### 模拟面试

```markdown
# 项目名称：金融高阶智能体后训练与强化学习对齐系统
# 角色：大模型后训练算法核心负责人
# 技术栈： PyTorch, DeepSpeed, Megatron-LM, vLLM, OpenRLHF, Qwen3.5-35B-A3B, GRPO/PPO, ReAct, Agent-DPO, Ray
# 项目描述：
	针对金融研报分析、数值计算、多工具调用中存在的“幻觉率高、复杂长链推理易陷入死循环、工具调用容错低”等问题，负责构建基于 Qwen3.5-35B-A3B 的高阶金融 Agent，完成从 **基础 SFT -> Agent 专属 SFT -> Agent RL（智能体强化学习）** 的全链路算法设计、数据合成与对齐调优。

# 核心工作：
	阶段一：金融领域基础 SFT (Domain Knowledge Injection)
		数据构建： 负责清洗并构建 15 万+ 高质量金融领域指令数据集（涵盖财报分析、合规风控、数值计算、研报生成等）。
		CoT 思维链注入： 在涉及计算与推理的数据集中引入思维链（Chain-of-Thought）与步骤显式化，设计动态 Mask 策略，显著增强模型在数值密集型任务上的基础逻辑能力。
	阶段二：Agent 工具调用与多轮决策 SFT (Agent SFT)
		轨迹数据合成 (Trajectory Synthesis)： 统一 ReAct 与 Function Calling 格式，搭建基于沙盒环境（Sandbox）的拒绝采样（Reject Sampling）数据生成管线。结合 Teacher Model 与真实金融 API 环境反馈，合成 5 万+ 包含“思考-动作-观察-修正”的多轮高质量 Agent 交互轨迹。
		损失函数优化 (Loss Masking)： 在多轮 Agent SFT 中，设计精准的 Loss Mask 机制，仅对模型产生的 Thought、Action 及 Final Answer 计算 Loss，屏蔽外部环境返回的 Observation（如 API 响应、SQL 返回结果），避免梯度污染并提升 Tool Use 格式遵从度。
	阶段三：基于环境反馈的 Agent 强化学习对齐 (Agent RL)
		环境沙盒与奖励机制设计 (Reward Model Design)： 构建包含金融 API、Python 执行器、数据库的交互沙盒，针对 Agent 长链规划设计多维度复合 Reward Function：
			环境结果奖励 (Environment Feedback Reward)： 代码/SQL 执行成功与否、计算结果对错（Rule-based Sparse Reward）。
			效率与过程奖励 (Format & Efficiency Reward)： 惩罚无意义的重复调用、冗余思考以及 JSON 格式错误。
			金融事实一致性奖励 (Factuality Reward)： 引入金融标的与数据校验 RM，惩罚幻觉。
		Agent 在线强化学习 (Agent Online RL)： 使用 GRPO (Group Relative Policy Optimization) / PPO 算法在沙盒中进行在线探索训练。重点解决 Agent 在 API 报错后的自适应修正能力 (Self-Correction)，使模型学会根据 Error Trace 重新修改 SQL 或 Python 代码。
		Agent 偏好对齐 (Agent-DPO)： 构建成功轨迹与冗余/失败轨迹的 Pair 对，采用 DPO 算法针对多轮上下文做偏好优化，在保持任务成功率的前提下将平均调用步数降低 30%。
		
# 项目成果与业务收益
	工具调用准确率 (Tool Call Acc)： 在金融专属工具集（10+类 API）上的 Function Calling 准确率从 Base+SFT 的 76.5% 提升至 95.2%。
	复杂推理与计算 (FinQA Benchmark)： 在金融计算与问答基准测试中，准确率相比开源基座提升 22.4%，数值计算幻觉率降低 65%。
	自我纠错能力： 在工具返回报错（如 SQL 语法错误、API 限流）场景下，Agent 的自我修复并完成任务的比率达到 84%。
	推理效率优化： 经过 Agent RL 的决策剪枝，单个复杂金融任务的 Token 消耗量降低 28%，大大缩短了 Agent 的响应延时（TTFT）。
```



#### 模拟问题

##### Q1:阶段一和阶段二在工程上是怎么训的？

```markdown
做法一：两阶段顺序训练 (Sequential Fine-tuning)
	流程：Base 模型 → 用阶段一的 15 万金融数据训 2-3 个 Epoch → 拿这个 Checkpoint 继续用阶段二的 5 万 Agent 数据微调 1-2 个 Epoch。
	优点：阶段划分清晰，便于控制变量做 Ablation（消融实验）。

做法二：混合多任务微调 (Mixed Multi-task SFT，大厂常用推荐)
	流程：阶段一（15 万数据）和阶段二（5 万 Agent 轨迹）按 3:1 的数据配比（Data Blending） 打包混合在一起，统一过 Data Loader 进行一轮大 SFT。
	优点：防止灾难性遗忘（Catastrophic Forgetting）。如果先单训知识再单训 Agent，模型可能会在第二阶段把第一阶段学到的复杂金融知识给“洗”掉。简历上写阶段一、阶段二是能力层面的模块划分，实际训练是 Data Mixing。
```



##### Q2:阶段三的奖励如何设计

```markdown
传统的 RLHF 用神经网络（Reward Model）打分，模型很容易学会“字数写得很长、废话很多、看似很有道理”来骗取高分（欺骗 RM）。
我们在金融推理中采用硬规则判卷：
	数值绝对匹配：通过正则表达式和 Python 的 SymPy 库，提取 <answer> 中的结果，与真实财报数据比对。数值不符直接给 0 分。
	格式约束奖励：必须严格包含 <think> 和 <answer> 标签，未闭合扣分。
```

------



### D. 金融级大模型高可靠抗幻觉推理系统 - 后训练,智能体（⭐）

#### 简历版

```markdown
# 项目名称：
金融级大模型高可靠抗幻觉推理系统（Agentic RAG）
# 技术栈：
PyTorch、Qwen3.5、Agentic RAG、FAISS/Milvus、vLLM、RAGAS、LoRA/SFT
# 项目概述：
针对金融复杂问答、信贷合规与智能风控场景中大模型的事实幻觉与逻辑推理幻觉问题，主导研发基于 Qwen3.5 基座 的分层检索增强、多阶段推理验证与自我反思（Self-Correction）高可靠推理框架，提升复杂多跳业务场景下的推理忠实度与可解释性。
# 核心职责：
1.可信推理流水线： 
	设计“意图解析→多粒度检索→链式推理→事实验证→动态反思”五阶段架构，引入任务分解与分步推理，支持复杂金融决策场景。
2.Agentic RAG 检索：
	# 可以先简单说一下 “Token级 - 句子级 - 段落级 - 文档级” 这块设计
	落地 Small-to-Big 多粒度架构，打通“Dense+BM25 混合召回 ➔ 滑动窗口语境扩展 ➔ Cross-Encoder 精排”流水线；结合 ReAct 动态路由与 HyDE 改写，规避长文本截断与“迷失（Lost-in-the-Middle）”，实现金融多跳（Multi-hop）证据的精准自主召回。
3.抗幻觉与自纠错： 
	融合 Chain-of-Verification（CoV）、Self-Consistency、Reflexion，构建“幻觉检测→证据补全→自我修正”闭环，提升回答事实性与推理可靠性。
4.领域模型微调：
	构建金融 CoT、批判与反思数据集，采用 SFT/LoRA 对 Qwen3.5 进行领域对齐，增强模型证据遵循、冲突识别与反思能力。
5.评测与工程优化： 
	基于 vLLM 优化推理部署，端到端 TTFT 降低 30%+；搭建 RAGAS + 自建金融基准评测体系，业务忠实度与准确率提升 25%+。
```



#### 模拟面试

```markdown
# 项目名称：
金融级大模型高可靠抗幻觉推理系统（Agentic RAG）
# 技术栈：
Python、PyTorch、Qwen3.5(MoE/Dense)、Agentic RAG、FAISS/Milvus、vLLM、RAGAS、LoRA/SFT
# 项目概述：
针对金融复杂问答、信贷合规与智能风控场景中大模型的事实幻觉与逻辑推理幻觉问题，主导研发基于 Qwen3.5 基座 的分层检索增强、多阶段推理验证与自我反思（Self-Correction）高可靠推理框架，提升复杂多跳业务场景下的推理忠实度与可解释性。
# 核心职责：
1.搭建多阶段可信推理流水线：
	设计“意图解析-多粒度检索-链式推理-事实验证-动态反思”五阶段协同架构，针对金融长文档与数值计算引入分步分解机制，支持复杂决策逻辑的闭环推导。
2.构建 Agentic RAG 检索引擎：
	设计基于 Small-to-Big（父子文档） 机制的多粒度索引架构，采用“Dense + Sparse 并发召回子切片 ➔ 动态滑动窗口扩展至父级语境 ➔ Cross-Encoder 语义精排”流水线；结合 ReAct 动态路由、Query 改写与 HyDE，有效避免“迷失在中间（Lost-in-the-Middle）”与语义截断，实现金融长文档多跳（Multi-hop）证据的自主探索与高精度召回。
3.实现验证链（CoV）与自纠错闭环：
	融合 Chain-of-Verification、多路径一致性采样（Self-Consistency）与反思智能体（Reflexion），构建“幻觉检测 → 靶向证据补全 → 自我修正”机制，将生成内容的事实性错误率大幅降低。
4.领域基座模型微调（SFT/LoRA）：
	针对推理与反思阶段，构建数千条金融高质量 CoT 推理与批判数据集，对 Qwen 基座进行指令微调与领域对齐，显著增强了模型遵循证据链与自主发现矛盾的能力。
5.工程落地与自动化评测体系：
	基于 RAGAS 与自建金融事实基准（Financial Benchmark）搭建自动化评测流水线；后端接入 vLLM 实现 PagedAttention 推理加速与动态批处理，系统端到端首字延迟（TTFT）降低 [30%+]，忠实度（Faithfulness）与回答准确率提升 [25%+]。
```



#### 模拟问题

##### Q1: SFT 数据集构建与训练关键技巧

```markdown
1.Loss Masking（损失掩码策略）：
	在训练时，只对 role: "assistant" 的部分计算 Cross-Entropy Loss，将 system 和 user 输入部分的 Token 的 Label 设为 -100。
2.数据分布配比（数千条黄金数据集）：
	40% 复杂多跳推理与计算 (Grounded CoT)：训练算术推导与引用规范。
	30% 冲突与拒绝回答 (Negative Samples)：当上下文中没有答案时，训练模型说“根据已知证据无法推断”，从源头遏制强行胡编。
	20% 验证链与自纠错 (CoV / Reflexion)：注入故意写错的数据对，训练模型做 Critic。
	10% 工具调用与意图路由 (Tool Calling)：训练模型输出 JSON/SQL 结构化查询参数。
3.数据清洗与构造方法（RLAIF + 规则）：
	基础数据由 GPT-4o 生成海量金融复杂问题，结合真实财报进行人工标注（Human-in-the-loop）校验；
	通过规则模板对数字、公司名、日期做 Entity Swapping（实体扰动）批量构造反思负样本。
```

Q2: 



Q3: 



Q4: 



## 7.1.2 **浪潮通信**（2021.07-2025.10）

大模型训推与异构算力适配平台 → 通用通信大模型训练  → 智能体平台；

### A. [大模型训推与异构算力适配平台]()

#### 简历版

```markdown
**项目名称**：大模型训推与异构算力适配平台
**技术栈**： Python、CUDA、CANN、MindIE、vLLM、SGLang、FastAPI、Docker、LoRA/QLoRA、模型蒸馏、LLM
**项目描述**：
	搭建覆盖数据处理、模型训练、模型蒸馏、推理部署及模型管理的一站式大模型平台，支持GPU、NPU（昇腾910B）、DCU等异构算力环境，实现LLM大模型、多模态大模型、Embedding及Reranker模型的统一训推与部署。累计完成10+模型跨平台适配，模型交付周期由3天缩短至1天以内，整体效率提升60%以上。

**核心工作**：
	1.数据与训练平台建设：自研数据标注与蒸馏工具，构建高质量训练数据流水线；封装PT、SFT、DPO、蒸馏等训练范式，支持Full Fine-tuning、LoRA、QLoRA等微调方案。
	2.异构算力适配：完成GPU、NPU及DCU环境适配，解决模型迁移适配、训练推理可用性验证、多机多卡性能测试和瓶颈分析。
	3.推理与平台化建设：集成vLLM、SGLang、MindIE等推理引擎，基于FastAPI与Docker实现模型工程化部署落地。
```



#### 模拟面试

```markdown
**项目名称**：大模型训推与异构算力适配平台

**技术栈**： Python、CUDA、CANN、MindIE、vLLM、SGLang、FastAPI、Docker、LLMs、LoRA/QLoRA、模型蒸馏、Embedding、Reranker、多模态模型

**项目描述**:
搭建面向企业级AI应用的一站式大模型训推与异构算力适配平台，覆盖数据处理、模型训练、模型蒸馏、推理部署、模型管理及跨硬件适配全生命周期能力。平台底层对接Kubernetes实现任务调度与资源编排，上层屏蔽GPU / NPU（昇腾910B）/ DCU等异构算力差异，统一提供模型训练与推理API能力。支持LLM、多模态模型、Embedding、Reranker及语音模型统一纳管，累计完成10+模型跨平台迁移与生产部署，将模型交付周期由3天缩短至1天以内，推理部署从1天缩短至30分钟，整体研发与运维效率提升60%以上。

**核心工作**:
1. 数据工程与模型蒸馏体系建设
    构建“数据生成 → 清洗 → 质量评估 → 回流优化”的闭环数据流水线，实现训练数据自动化生产
    基于教师模型（LLM）构建CoT级知识蒸馏机制，自动生成高质量问答对与推理链数据
    设计多维数据质量评估指标（语义重复度、困惑度、任务一致性），提升数据有效性
    将低质量人工标注依赖降低 50%+
2. 多范式训练平台研发
    统一抽象 PT / SFT / DPO / PPO / GRPO 多类训练范式，构建可插拔训练执行框架
    支持 Full Fine-tuning / LoRA / QLoRA 自动策略选择（基于显存与任务类型）
    设计训练任务编排系统（Pipeline化），支持断点恢复、失败重试与任务优先级调度
    实现训练过程指标可观测（loss / grad norm / lr / GPU util）
3. 异构算力适配与国产化迁移
    在昇腾910B环境下完成浪潮、百川等大模型的推理与微调迁移
    基于MindIE/MindFormers解决模型加载、权重转换及训练推理兼容问题，支持LoRA/SFT等训练验证
    完成单机多卡与多机多卡性能测试与瓶颈分析
4. 高性能推理与服务部署优化
    集成vLLM、SGLang、MindIE等主流推理引擎，实现不同硬件环境下推理框架统一调度；
    优化 KV Cache / batch 合并 / continuous batching 提升吞吐能力
5. 模型资产管理与平台化建设
    构建统一 Model Registry（LLM / Embedding / Reranker / 多模态）
    提供 FastAPI 标准化服务接口 + Docker 化部署链路
    
**项目成果**
    支撑GPU、NPU、DCU三类异构算力环境统一管理；
    完成10+模型跨平台适配与生产部署；
    模型交付周期由3天缩短至1天以内；
    模型部署周期由1天缩短至30分钟；
    运维成本降低50%，整体研发与交付效率提升60%以上；
    支撑多个企业级AI应用场景快速落地与迭代。
```



##### 1. 代码与环境迁移

- **遇到什么：** 原始代码中充斥着大量的 cuda 字眼，比如 model.to('cuda')、inputs = inputs.cuda()，并且使用了 NVIDIA 生态的分布式训练工具。

- **怎么做：**

  - 我们在昇腾环境中安装了对应的 CANN 驱动包和 torch_npu 插件。

  - 在训练启动脚本（Python 代码）的最顶部加入 import torch_npu。

  - 把代码中的 device = 'cuda' 统一修改为 device = 'npu'。对于多卡分布式训练，我们将 PyTorch 原生的 nccl 通信后端修改为昇腾支持的 **hccl**（Huawei Collective Communication Library）：

    ```python
    # 原始代码
    # torch.distributed.init_process_group(backend="nccl")
    # 修改后
    torch.distributed.init_process_group(backend="hccl")
    ```

  - 通过这样简单的几步替换，让代码能够在 NPU 环境上跑起来不直接报错报错。

##### 2. 算子兼容性解决

- **遇到什么：** 代码跑起来后，在第一步的前向传播时突然崩溃，报错显示某个算子在 NPU 上不支持。排查后发现，LLaMA 架构中使用了 **RMSNorm（均方根归一化）**，而原始代码中导入的是一个针对 NVIDIA GPU 编写的自定义 CUDA Kernel（通常由 Triton 或 C++ 编写）。
- **怎么做：**
  - 由于这个特殊的 CUDA Kernel 无法直接在 NPU 上运行，我们首先采取了**临时替代方案**：用 PyTorch 原生的基础数学算子拼装了一个 Python 版的 RMSNorm（包含 mean、pow、sqrt 等基础算子）。
  - 虽然拼装版的算子运行效率较低，但它不依赖特定的 GPU 硬件，成功让模型顺利避开了报错，完成了前向和反向传播的初步打通。



##### 3. 精度对齐

- **遇到什么：** 模型虽然能跑了，但我们发现微调了几个 Step 之后，**训练 Loss 突然变成了 NaN（数值溢出）**，或者 Loss 下降的速度和在 A100 上完全对不上，出现了严重的精度偏差。
- **怎么做：**
  - 我们使用昇腾提供的**精度比对工具（Precision Debugger）**，在 A100 和 910B 上用完全相同的输入数据和权重，分别跑了一次前向传播，并 Dump 出每一层算子的输出 Tensor。
  - 通过工具的余弦相似度比对，我们发现在第 12 层的 Attention 计算中，由于使用了 bfloat16 精度，昇腾底层的某些指数计算（如 Softmax 内部）因为溢出保护机制的微小差异，导致数值产生了极大的偏差。
  - **解决手段：** 我们在代码中对该 Attention 的 Softmax 局部计算进行了“精度提升”，即在前向传播计算 Softmax 时，显式地将 Tensor 转换为 float32，计算完成后再转回 bfloat16 发送给下一层。重新训练后，Loss 曲线终于恢复正常，与 A100 的收敛趋势基本一致。



##### 4. 性能调优

- **遇到什么：** 精度对齐后，模型可以稳定训练了。但我们发现单步训练耗时极长，吞吐量（Tokens/second）大概只有 A100 的 35% 左右，这显然不符合 910B 的实际算力表现。
- **怎么做：**
  - 我们开启了 **Ascend Profiler** 抓取了一段训练的 Timeline（时间线）。
  - 通过可视化分析发现：
    1. 之前我们在步骤 2 中用 PyTorch 基础算子拼装的 RMSNorm，在底层被拆分成了十几个小算子频繁下发，产生了大量的 **AICPU 算子调用和 Host-Device 之间的内存拷贝**，成为了严重的性能瓶颈。
    2. Attention 计算没有利用硬件的高带宽优势。
  - **解决手段：**
    - 我们用 torch_npu 内置的高性能融合算子 torch_npu.npu_rms_norm 替换掉了我们自己拼装的 Python 版算子。
    - 引入了昇腾原生支持的 **FlashAttention-2**（通过 npu_fusion_attention 或 npu_prompt_flash_attention 接口），将矩阵乘法、Softmax、Dropout 融合成一个大算子直接在 AICore 内完成计算。
  - 经过这两项优化，算子下发频率大幅降低，AICore 始终处于饱满的工作状态。最终，模型的训练吞吐量提升了数倍，达到了预期的性能指标。



#### 模拟问答

###### Q1：简单介绍这个项目？

```
面试官好，我重点介绍一下异构算力训推平台中的推理部署与算力适配部分。

这个项目的核心目标是解决多硬件生态下大模型推理成本高、吞吐低、跨芯片适配周期长的痛点。我们集成了 vLLM、SGLang 和华为 MindIE，构建了统一的推理服务层，完成了英伟达 GPU、华为昇腾 910B 以及海光 DCU 的异构适配。

在这个过程中，我主要攻克了昇腾 910B 算子适配与 MindIE 性能调优、SGLang Prefix Caching 在长文本/多轮对话下的吞吐优化，以及跨芯片推理吞吐/延迟指标的标准化。最终将模型上线适配周期从 3 天缩短到 1 天以内，特定场景推理 TPS 提升了 2 倍以上。
```



###### Q2：昇腾如何分析性能瓶颈？

```markdown
# 昇腾用 MindStudio Profiler / Ascend Insights。分析算子 Execution Time（计算受限/Memory Bound）与 Communication Time（AllReduce 阻塞）。
msprof 采集的数据最终会呈现为 Timeline 轴线图（msprof.json） 和 算子明细表（op_summary.csv）。
在实际排查中，我最常看两类指标：
	在 Timeline 上看 Host 与 Device 之间的 Gap，用来定位 Python 侧下发慢或同步阻塞（Host-Bound）；
	在 CSV 表格里看 Cube 利用率与 HBM 读写带宽，用来区分算子是计算受限（Compute-Bound）还是访存受限（Memory-Bound）。

瓶颈 1：Host-Bound（Host 侧下发瓶颈 / NPU 频繁空转）
	在 Timeline 上的表现：Top 轨（Host 侧）忙得不可开交，而 Bottom 轨（NPU Device 侧）的 Kernel 之间存在大量的空白间隙（NPU Idle）。
	原因：
		Host 侧 Python 代码存在慢操作（如 CPU 上的复杂数据处理、频繁 print/日志记录）。
		代码中滥用了 torch.cuda.synchronize() 或同步拷贝，强行让 NPU 停下来等 CPU。
		小算子太密集，Host 侧下发的速度赶不上 NPU 计算的速度。

瓶颈 2：小算子下发开销大（Launch Overhead）
	在 Timeline 上的表现：NPU 轴线上全是密密麻麻如“针尖”般的小 Kernel（执行时间仅 12us），但算子与算子之间的切换间隙却长达 510us（调度开销比执行时间还长）。
	原因与解法：模型未做算子融合。需要开启 CANN 图模式、使用 MindIE/ATB 融合算子，或者用 Ascend C 重新编写融合内核。

瓶颈 3：Memory-Bound（访存密集型算子瓶颈）
	在 op_summary.csv 上的表现：LayerNorm、Softmax、RMSNorm、GeLU 等算子单次耗时很高；Vector Pipe Utilization 很高，且 Read/Write Bandwidth 接近 HBM 理论带宽峰值（如 910B 接近 1.6 TB/s），但 Cube Utilization 为 0。
	原因与解法：AI Core 的计算单元在等待数据从 HBM 显存加载到 L1/L2 Cache。需要将“访存-计算-访存”的多步操作融合成一个 Kernel，减少对 HBM 的往返读写。

瓶颈 4：Cube 利用率低 / 算子未对齐硬件（Compute Underutilization）
	在 op_summary.csv 上的表现：MatMul（矩阵乘）算子占用了大幅时间，但 Cube Pipe Utilization 只有 20%~30%。
	原因：
		Shape 未对齐：输入的 Batch Size 或 Hidden Size 没有按昇腾推荐的 16 / 32 / 128 字节/元素对齐，导致 AI Core 内部无法满矩阵填充计算。
		动态 Shape 频繁触发重编译：频繁地动态改变 Sequence Length。

瓶颈 5：通信未掩盖（Communication Non-Overlap）
	在 Timeline 上的表现：在多卡（TP/PP）场景下，通信 Kernel（如 HcomAllReduce）单独占据了一整块时间轴，前后完全没有 AI Core 的 Compute Kernel 在并行执行。
	原因与解法：没有实现计算与通信的重叠（Overlap）。需要检查流水线并行（PP）的 1F1B 策略或张量并行（TP）的 Ring-AllReduce 是否正确配置了异步通信流（Stream）。
```



###### Q3：你们在昇腾 910B 上做推理部署，遇到最难的性能/兼容性坑是什么？怎么解决的？

```markdown
1. 讲一个具体的坑，比如 FlashAttention 算子融合与动态 Shape 导致的图编译卡顿。
2. 过程：在 910B 采用 Graph 模式时，每次 Sequence Length 变化都会触发重新编译（JIT Compilation），导致极高的延迟。
3. 解决：引入 MindIE 的 Dynamic Shape Padding/Bucket 机制（设置阶梯式的 SeqLen Bucket，如 256, 512, 1024, 2048），减少图编译触发；
4. 同时通过 Ascend C 自定义融合算子，将 Layernorm + QKV Projection 进行融合，减少内存 IO 访存频次。
```



###### Q4：如何昇腾上适配新的LLM模型？

```python
# 这套适配流程可以总结为 5 个标准阶段：算子评估 -> 模型组图 -> 权重映射 -> 精度对齐 -> 性能调优。
# 第一步：可行性打通与算子替换（跑通 Eager 模式）
目标：不考虑性能，先让模型在 NPU 上使用 PyTorch（torch_npu）跑通一次 Forward/推理。
1.算子兼容性排查（核心攻坚）：
    优先替换为 torch_npu 官方高性能算子：例如将自定义的 FA2 替换为 torch_npu.npu_flash_attention；将 RMSNorm 替换为 torch_npu.npu_rms_norm；将 RoPE 替换为 torch_npu.npu_rotary_mul。
    降级为 PyTorch 原生算子 (ATen)：若无对应硬件加速算子，先用纯 PyTorch 算子重写逻辑（如用基础的 torch.matmul + softmax 替代 Attention），确保逻辑正确。

# 第二步：精度对齐（逐层输出比对）
目标：确保 NPU 上跑出来的 Token 输出和 GPU（或 HuggingFace 原生）完全一致，防止出现“能跑但输出乱码”的情况。
1.环境对齐：
	固定随机种子（torch.manual_seed）。
	推荐统一使用 BF16 数据类型（昇腾 910B 对 BF16 原生支持极佳，FP16 在深层 Transformer 中易发生梯度/数值溢出）。
2.逐层 Hook 抓取 Tensor 与余弦相似度（Cosine Similarity）比对：
	编写 Hook 脚本，分别在 GPU（基准）和 NPU 上输入完全相同的 Prompt。
	依次抓取并比对以下节点的 Output Tensor：
		Embedding 层输出
		Transformer Layer 1（Self-Attention 前/后、MLP 前/后）
		Final LayerNorm 输出
		LM_Head 的 Logits
	判定标准：每一层的 Cosine Similarity 应大于 0.999。若某一层相似度突降（如从 0.99 掉到 0.85），则锁定了产生精度漂移的“问题算子”。
    
# 第三步：高性能推理引擎集成（入库 vLLM-Ascend 或 MindIE）
1.路线 A：集成到 vllm-ascend（社区/通用路线）
    注册新模型架构：在 vllm/model_executor/models/ 下新建模型文件。
    替换为 vLLM 逻辑层：
    	将 nn.Linear 替换为 vLLM 的 ColumnParallelLinear / RowParallelLinear（实现多卡张量并行 TP）。
    	将 Attention 替换为 vLLM 的 Attention 模块，其底层会自动调用昇腾适配的 PagedAttention NPU Kernel。
    适配 Weight Loader：编写权重加载逻辑，映射 HuggingFace 的权重 Key 名。
2.路线 B：集成到 MindIE / ATB Models（华为官方最高性能路线）
    如果寻求极致吞吐，需接入昇腾 ATB (Ascend Tensor Boost) 加速库：
    定义 ATB 图结构：使用 C++ 或 ATB Python API 组装模型的计算图（定义 Tensor Node 和 Operation Node）。
    调用融合 Operation：直接调用 ATB 预置的 LinearParallelOperation、PagedAttentionOperation、RmsNormOperation。
    MindIE Service 打通：将 ATB 模型打包为 .so 动态库，配置 config.json，由 MindIE Service 统一加载进行 Continuous Batching 管理。

# 第四步：性能 Profiling 与瓶颈调优（跑快）
模型能上线后，使用 msprof / MindStudio Profiler 进行最终的性能打磨。
1.消除动态 Shape 导致的重编译（JIT Overhead）：
	问题：NPU 图模式对动态 SeqLen 非常敏感，每次长度变化都可能触发 CANN 重新编译图。
	优化：开启 Bucket（分桶）机制（如 256, 512, 1024），将输入 Padding 到固定的 Bucket 长度，或者使用 Chunked Prefill 固化计算 Shape。
2.提升 AI Core 算子利用率：
	查看 op_summary.csv，确保 MatMul 和 Attention 的 Cube Pipe Utilization 在 60% 以上。
3.多卡通信重叠（HCCS Overlap）：
	在多卡张量并行（TP）下，利用 Profiler 查看 HcomAllReduce 通信算子是否与前后的 MatMul 计算重叠，调整通信 Stream 优先级。

```



###### Q5：既然有了 vLLM，为什么推理平台还要引入 SGLang？

```python
1. 场景分化：vLLM 是极佳的“通用吞吐型引擎”，但在 Agent 和 RAG 场景有局限。

2. SGLang 的优势：Agent 存在大量的多轮 Task 和复杂的 System Prompt（如 2k Token 的工具定义）。SGLang 提出了 RadixAttention，将历史 KV Cache 用 Radix Tree 维护。当多个 Agent 请求共享同一套 Tool Definitions 时，SGLang 可以 100% Hit Cache，免去 Prefill 阶段开销。

3. 受限生成：Agent 需要强约束输出 JSON。vLLM 的 Outlines 约束解码较慢，而 SGLang 的 FSM 约束生成直接在 CUDA Kernel 级优化，JSON 输出速度提升明显。
```

###### Q6：CUDA -> CANN

```
踩坑：很多开源模型包含了自定义的 CUDA C++ Kernel（如 FlashAttention-2、Rotary Embedding、Triton 算子），在昇腾上无法直接运行。
解法：替换为昇腾 CANN 提供的 torch_npu 适配库算子，或调用 Ascend C / MindSpore 算子库；使用昇腾的 atb (Ascend Tensor Boost) 加速库。
```



###### Q7：模型在 GPU 上收敛/推理正常，迁到 910B 上出现 Loss NaN 或者输出乱码。

```
1. FP16/BF16 算子表达差异：昇腾 910B 对 FP16 的溢出范围较为敏感，开启全链路 BF16 训练/推理。

2. 逐层梯度/激活对比工具：自研/利用对比工具，在同一输入下，hook 出 GPU 和 NPU 每一层 Transformer Block 的 Output Tensor，计算余弦相似度（Cosine Similarity），定位出精度离群（Outlier）的算子（如 LayerNorm 或 Softmax 边界条件处理差异）。
```



###### Q8：你说自动生成数据，怎么防幻觉？

```markdown
三重过滤机制：
rule filter
	regex / schema validation
model judge
	用 teacher model 做 consistency check
embedding filter
	去除低相似 / off-topic sample
🔥追问：
    teacher hallucination怎么办？
    数据质量怎么量化？
```

###### Q9：蒸馏具体怎么做？

```markdown
我们做的是：
    logit distillation + instruction distillation
    loss = CE + KL(student || teacher)
重点优化：
    引入 CoT distillation（只保留 reasoning skeleton）
    降低冗余推理路径
🔥追问：
    CoT会不会泄露噪声？
    student capacity不够怎么办？
    	引入“助教”模型：先用教师模型（大）蒸馏助教模型（中），再用助教模型（中）蒸馏学生模型（小）。
    	优化损失函数与超参数调节。
    	采用渐进式蒸馏：多阶段训练、分层逐步蒸馏。
    	优化特征对齐（Feature-based KD）的策略。
    	改进学生模型自身的网络设计。
    	样本筛选：过滤掉教师模型自身也极其不确定（熵过高）的样本。
```

###### Q10：你在昇腾 910B 上做推理部署时，遇到过最大的性能瓶颈是什么？怎么解决的？

```markdown
1. 现象：最初直接用 PyTorch-NPU 部署，高并发下 GPU 侧能跑 1000 QPS 的模型，在 910B 上只有 200 QPS，且首包延迟很高，NPU-Util 波动大。
2. 排查：通过 CANN 的 msprof 工具抓取 Profiling 性能数据，发现两大问题：一是不定长 Prompt 导致频繁触发 ATC 编译/算子 Shape 重构建；二是 KV Cache 在 NPU 显存中存在严重的动态分配开销。
3. 解决：切换到华为 MindIE Engine；开启 Continuous Batching 和 Padding 优化，配置固定的 Block Size 实现 PagedAttention；利用 HCCL 优化了 TP=2/TP=4 时的多卡通信开销。最终吞吐提升了 3.5 倍，首包延迟降低 60%。
```

###### Q12：推理性能指标？

```markdown
标准回答模板：
    QPS：xxx
    P95 latency：xxx ms
    tokens/s：xxx
    GPU utilization：xx%
🔥追问：
    baseline是多少？
    提升来自哪里？
```

###### Q14：你这个“训推与异构算力平台”整体是解决什么问题的？

```markdown
这个平台主要解决企业在大模型落地过程中三个核心问题：
    第一是模型全生命周期管理碎片化，从数据、训练、评估到部署缺乏统一链路；
    第二是异构算力环境复杂，包括GPU、昇腾NPU、DCU，不同框架（CUDA/CANN）导致模型迁移成本高；
    第三是推理与训练系统割裂，训练与推理各自独立，缺乏统一调度与模型资产管理能力。

因此我们构建了一个统一平台，把数据、训练、推理和部署统一成一个闭环系统。
```

###### Q17：你觉得你这个项目最大的技术难点是什么？

```markdown
最大难点不是单点技术，而是：
	“异构算力 + 多模型 + 多推理引擎”的统一抽象

核心挑战：
    不同框架语义不一致
    推理路径复杂
    性能优化需要跨层协同
```

###### Q18：数据质量核心指标详解：语义重复度、困惑度 PPL、任务一致性

```
1、语义重复度，基于向量语义相似度判断两条文本内容是否高度重复

2、困惑度，衡量**大语言模型对一段文本的流畅度、自然度、逻辑通顺程度的量化指标**，衡量文本是否符合人类语言习惯、有无乱码、断句错乱、无意义拼接、OCR 噪声、机器乱生成文本。

3、任务一致性，衡量**文本内容与当前业务任务目标的匹配程度**，判断数据是否属于业务领域、是否偏离任务、有无无关噪声、是否符合数据采集规范。
```



###### Q19：深节点 1：昇腾 910B + CANN + MindIE 的踩坑与性能调优

```
在 NPU（910B）上部署时，我们没有止步于原生 PyTorch 转换，而是深度引入了 CANN 架构和 MindIE 推理引擎。
面对部分自定义算子或特定模型结构（如 Qwen/Llama 变体）在昇腾上的算子缺失问题，我们做了算子替换与 MindIE-LLM 侧的接驳；
通过配置 Continuous Batching、PagedAttention 以及 Task-level 的并行策略，在 910B 上将 FP16 推理吞吐提升到了接近 A100 的 85%-90% 水平。
```



###### Q20：vLLM vs. SGLang 场景化选型与内存/KV Cache 优化

```
在 GPU 侧，我们同时集成了 vLLM 和 SGLang。vLLM 负责常规高并发通用 Token 生成；
而对于多轮 Agent 对话、带 System Prompt 的 RAG 场景以及复杂 JSON 强制格式输出场景，我们切换到了 SGLang。
利用 SGLang 的 RadixAttention（基数树 KV Cache 重用） 和 Compressed Finite State Machines（受限生成），使 Agent 场景下的首包延迟（TTFT）降低了 40%，整体吞吐提升了 50%。
```



Q20：

------



### B. 通信基站与机房硬件故障多模态排障 Agent 系统 - 多模态,智能体

#### 简历版

```markdown
# 项目名称：
通信基站与机房硬件故障多模态排障 Agent 系统
# 技术栈：
Python, PyTorch, DeepSpeed ZeRO-3, Qwen-VL, MM-CoT, DPO/SimPO, Visual Grounding, ReAct/Reflexion, Multimodal RAG
# 项目概述：
针对通信/智算机房硬件故障定位难、排障链路长痛点，主导研发从垂直多模态大模型底座训练、偏好对齐到排障 Agent 落地的全流程系统。
# 核心职责：
1.多模态后训练与 MM-CoT：
	构建 120 万条图文/时序日志数据集；设计“视觉定位(BBox) → 日志对齐 → 根因推导”推理链，结合高分辨率局部切片（Patch Crop），将硬件缺陷定位 mAP@0.5 提升至 89.4%（+32.4%）。
2.算力加速与抗幻觉对齐：
	基于 DeepSpeed ZeRO-3 + FlashAttention-2 优化显存，训练吞吐提升 1.8 倍；提出融合 Visual Token 加权与 IoU Loss 的 DPO/SimPO 偏好对齐，使指示灯及状态误判等幻觉率降低 41.5%（POPE 评测）。
3.拓扑 RAG 与 Agent 闭环落地：
	基于 ReAct + Reflexion 自纠偏架构集成 15+ 运维 API，结合机房拓扑 Multimodal RAG，实现自主排障成功率 81.6%，推动一线平均修复时长（MTTR）从 48 分钟降至 11 分钟。
```

#### [模拟面试](https://chatgpt.com/c/6a2d0d19-d93c-83ea-83d5-c276de57e2eb)

```markdown
# 项目名称：
通信基站与机房硬件故障多模态排障 Agent 系统
# 技术栈：
Python, PyTorch, DeepSpeed ZeRO-3, Qwen-VL, MM-CoT, DPO/SimPO, Visual Grounding, ReAct/Reflexion, Multimodal RAG
# 项目概述：
针对通信基站与智算机房运维中“设备板卡故障定位难、告警日志跨系统关联慢、排障链路长”的痛点，主导研发垂直领域多模态大模型底座与自动化排障 Agent 系统。完成从领域数据构建、MM-CoT微调、多模态偏好对齐、分布式算力加速到上层 Agent 工具调用的全流程落地。
# 核心职责：
1.多模态领域后训练与 MM-CoT 设计：
	构建 120 万条涵盖硬件图像（板卡/光纤/走线）与时序日志的高质量微调集；
	设计 <视觉定位(BBox) -> 日志时序对齐 -> 故障根因推导 -> 决策动作> 的 MM-CoT 推理链；针对光纤错插与微小缺陷，引入高分辨率局部切片（Patch Crop）与全局特征融合微调，显著提升细粒度特征感知力。
2.分布式训练加速与显存优化：
	基于 DeepSpeed ZeRO-3 与 FlashAttention-2 搭建多机多卡混合精度训练流水线，针对动态分辨率图像切片优化显存开销，训练吞吐提升 1.8 倍。
3.多模态抗幻觉与偏好对齐（DPO / SimPO）：
	针对指示灯状态误判（如虚假红灯）与高危误操作，构建专家黄金轨迹 vs 错误诱导排障偏好样本库；
	提出在损失函数中融入 Visual Token 局部加权与坐标 IoU Loss 惩罚，结合 DPO/SimPO 优化对齐目标，将模型关键指标幻觉率降低 41.5%（POPE 评测）。
4.多模态 Agent 架构与拓扑 RAG 集成：
	基于 ReAct + Reflexion 自纠偏机制构建智能体，集成 NetConf/SNMP 及端口重置等 15+ 运维 API；
	构建机房时空拓扑图谱向量库（Multimodal RAG），实现复杂状态机下的多轮自主排障与错误自纠偏。
5.量化成果：
	硬件缺陷定位准确率（mAP@0.5）达 89.4%（较基座提升 32.4%），结构化 JSON 与坐标格式遵循率达 99.2%；
	Agent 自主排障成功率达 81.6%（平均 3.8 步调用），推动一线工单平均修复时长（MTTR）由 48 分钟缩减至 11 分钟。
```



#### 模拟问题

##### Q1：



##### Q2：



### C. 无线基站智能排障与自主网优 Agent 系统 - 多模态,智能体

#### 简历版

```markdown
# 项目名称：
# 角色：
# 技术栈： 
# 项目描述：
# 核心工作：
```



#### 模拟面试

```markdown
# 项目名称：
无线基站智能排障与自主网优 Agent 系统 
# 技术栈： 

# 项目描述：

# 核心工作：
1.多模态通信感知与表征对齐：
	针对射频干扰、天馈错接等问题，采用 ViT + Qwen2-VL 构建视觉特征提取器，实现对 RF 时频瀑布图（Interference Spectrogram）与星座图的异常模式分类与热区定位；
	融合 PatchTST 时序异常检测模型（处理 RSRP/SINR/CQI/BLER 等高频 KPI 序列）与告警文本，基于跨模态投影层（Projector）实现时序-视觉-文本的统一 Embedding 空间映射，跨模态表征检索准确率达 93.5%。
2.通信 Agent 认知决策与工具调用架构设计：
	基于 ReAct + Plan-and-Solve 双层架构设计 Agent 决策大脑，构建涵盖“基站拓扑过滤查询、配置下发沙箱验证、MR 聚合分析、覆盖仿真算法”等 15+ 通信领域原子工具链；
	引入 Constrained Decoding 与 JSON Schema 强约束机制，结合 Self-Reflection 纠错逻辑，将复杂排障任务下的多轮 Tool Calling 成功率从基线 64% 提升至 93.2%。
3.百万级工单 RAPTOR 树状与多向量混合检索体系（核心检索创新）：
	分级抽象索引构建：针对百万级历史工单，弃用传统图谱的高昂维护方案，改用 RAPTOR 树状递归聚类架构，通过 Gaussian Mixture Models (GMM) 对海量底层工单进行递归聚类与 LLM 摘要总结，生成“宏观故障根因层 - 典型场景类簇层 - 微观工单处置层”三层树状索引；
	多路异构精准检索：构建 Elasticsearch 结构化硬过滤（锁定网元型号/告警码/频段）+ BGE-M3 密集/稀疏混合向量召回 + ColBERTv2 细粒度 Token 级迟交互（Late Interaction） 的三路召回流水线；
	Query 增强与精排重构：引入 HyDE（Hypothetical Document Embeddings） 解决现场口语化描述与规范工单术语不一致问题，结合 Cross-Encoder（BGE-Reranker-Large） 二次精排，使疑难多并发故障的上下文召回命中率提升 28%。
4.量化成果与性能优化：
	采用 vLLM + PagedAttention 进行推理加速与多并发优化，结合分级缓存策略，将单次复杂排障决策的端到端 P99 延迟控制在 2.1 秒内；
	系统在模拟网及现网试点验证中，将基站突发干扰及弱覆盖问题的平均修复时间（MTTR）降低约 45%，根因定位准确率（Top-1 Accuracy）达到 89.1%，显著提升了网络自主运维水平。
```



#### 模拟问题



### D. 通信领域大模型训练与对齐优化 - 后训练（⭐）

#### 简历版

```markdown
# 项目名称：
通信领域大模型训练与对齐优化
# 技术栈：
PyTorch、QLoRA、LoRA、Post-Train、SFT、DPO、分布式训练、模型评测体系
# 项目概述：
基于开源大模型构建通信行业专属大模型训练体系，完成 Post-Train 的持续预训练、SFT监督微调及DPO偏好对齐全流程研发；搭建数十GB通信领域语料库与多任务指令数据集，采用 LoRA + DeepSpeed 实现低成本训练；构建专业评测体系与数据闭环优化机制，最终通信场景问答准确率提升15%，幻觉率下降7%，模型成功落地智能运维、故障诊断、招投标分析等多个生产场景。
# 核心职责：
    1.	搭建通信领域 Post-Train → SFT → DPO 完整训练流水线； 
    2.	构建数十GB领域语料、数万条 SFT 数据及偏好对齐数据集； 
    3.	基于 LoRA + DeepSpeed 实现高效增量训练，显著降低训练成本； 
    4.	建立自动评测与数据回流闭环，持续优化模型专业能力与稳定性。
```



#### 模拟面试

```markdown
**项目名称**：通信领域通用大模型训练与对齐优化
**技术栈：**PyTorch、QLoRA、LoRA、Post-Train、SFT、DPO、分布式训练、模型评测体系
**项目概述**：针对通信行业算网运维、通信故障诊断、招投标分析、财务稽核等核心场景，基于 Qwen2.5-32B 开展领域化训练与能力对齐，构建覆盖“领域知识注入→指令理解→偏好对齐”的全流程训练体系；最终相较原生开源底座模型，业务场景问答准确率提升15%，模型幻觉率降低7%，故障排查场景任务完成率提升12%。
**核心职责**：
	1.负责通信领域大模型 Post-Training 流水线设计与落地，打通“领域持续预训练 → SFT 指令微调 → DPO 偏好对齐 → 自动评测 → 数据回流”的完整研发链路。
    2. 搭建通信领域语料处理体系，整合行业文档、历史故障工单、招投标资料、运维手册等内外部私有数据，分别构建面向领域知识注入的 Post-train 无标注语料，高质量通信专属 SFT 训练数据集，以及构建通信领域偏好 DPO 数据集；`设计完整数据处理流水线，完成文本解析(PDF/Word/HTML等异构文档解析)、格式统一、脏数据过滤、相似度去重(MinHash+Embedding相似度去重)、业务数据脱敏、高质量样本筛选等工作。
    3. 通信领域持续预训练（Continued Pre‑Training）:构建通信专业语料训练集、实现领域增量训练Pipeline、设计学习率Warmup与Cosine Decay策略、采用QLoRA + DeepSpeed降低训练成本、监控Loss、Perplexity等训练。对比实验：LoRA 和 全量参数
    4. 多阶段监督微调（SFT）:设计并构建覆盖故障定位问答、运维工单分析、CLI指令解析、招投标信息抽取、财务规则审核、专业知识问答等场景的多任务 SFT 数据集，累计整理数万条高质量指令样本。第一阶段：增强指令跟随、格式遵循和多轮对话能力；第二阶段：重点强化通信术语理解、故障分析推理和专业知识生成能力。
    5. 强化学习对齐（DPO）：构建基于专家规则、模型辅助和人工审核的偏好数据生产流程，设计评价维度：专业准确性、事实一致性、推理完整性、回答可执行性、幻觉控制
    6. 模型评测与迭代：搭建通信领域专项评测体系。通用能力：BLEU、ROUGE、MT-Bench；专业能力：通信知识问答准确率、故障定位成功率、工单解析准确率、招投标信息抽取准确率；安全与稳定性：Hallucination Rate、Consistency Score、Refusal Rate。建立：数据分析、模型训练、自动评测、错误归因、数据优化、重新训练的持续迭代优化闭环。
    7. 建立线上问题采集、难例挖掘、人工复核、数据增强、增量训练和版本回归机制，形成模型效果持续优化的数据闭环。
```



#### 模拟问题

##### Q1：请先介绍一下你这个项目，重点讲清楚业务背景、目标以及你负责的部分。

```markdown
这个项目的目标是构建通信领域专属大模型，覆盖算网运维、通信故障诊断、招投标分析、财务稽核等多个业务场景。

项目启动时存在三个核心问题：
    通用大模型缺乏通信领域知识，对于专业术语和故障场景理解不足；
    对通信运维流程缺乏认知，回答容易出现幻觉；
    无法满足企业内部业务要求，例如工单分析、招投标审核等专业任务。

因此我们设计了：
领域数据构建
      ↓
Post-Train领域知识注入
      ↓
SFT任务能力训练
      ↓
DPO偏好对齐
      ↓
专项评测体系
      ↓
持续迭代优化
```

##### Q2：为什么要采用 Post-Train + SFT + DPO 三阶段训练，而不是直接SFT？

```markdown
因为三者解决的问题不同。
# Post-Train（属于知识缺失问题）
解决：模型不知道通信知识
例如：BBU是什么？AAU是什么？OTN和PTN区别？

# SFT（属于任务能力问题）
解决：知道知识，但不会按照任务要求使用
例如：根据告警日志定位故障，按照JSON格式输出

# DPO（DPO负责学习偏好）
解决：多个答案都正确，哪个更符合专家偏好
例如：
故障分析：
答案A：
	检查光功率
答案B：
    先检查告警等级
    再检查链路状态
    最后验证光功率
显然B更符合专家经验。
```

##### Q3：为什么选择DPO而不是RLHF？

```markdown
原因1：实现复杂度
原因2：训练稳定性
原因3：数据现状
```



##### Q6：通信数据是怎么构建的？

```markdown
主要来源：
    运维手册
    历史工单
    故障案例库
    招投标文件
    规范标准
    FAQ知识库
    
数据处理流程：
    文档解析
    ↓
    清洗
    ↓
    去重
    ↓
    脱敏
    ↓
    质量评估
    ↓
    训练集
```

##### Q7：为什么使用MinHash+Embedding双重去重？

```markdown
因为单纯MinHash无法解决语义重复。

第一层：MinHash，速度快。
第二层：Embedding，计算向量相似度。
```

##### Q8：Embedding阈值怎么定？

```markdown
测试：
    0.80
    0.85
    0.90
    0.92
    0.95
随机抽取：人工判断是否重复。
```

##### Q9：为什么选择QLoRA而不是全量微调？

```markdown
核心原因：成本
```

##### Q10：QLoRA为什么不会明显掉点？

```markdown
训练时：
基础权重冻结
但前向传播仍使用完整知识。

实际更新的是：
ΔW = BA
低秩矩阵。

对于通信场景：
知识迁移量有限。
LoRA已经足够。
```

##### Q11：训练过程中遇到过什么线上问题？

```markdown
问题1：灾难性遗忘
    通信能力提升，通用能力下降
    原因：
        领域数据占比过高
    解决：
        加入20%通用数据混训

问题2：格式幻觉
	要求JSON输出。模型输出：解释文字+JSON
    解决：
        增加格式约束SFT样本
    并引入：
        Format Reward

问题3：故障分析逻辑跳步
	直接输出结论。没有排查过程。
    解决：
        加入：CoT数据
        例如：告警分析 → 链路分析 → 设备分析 → 故障定位
```

##### Q12：问答准确率提升15%，这个指标怎么测出来的？

```markdown
构建独立测试集：
    通信知识问答，2000条，专家人工标注标准答案。

评测方式：
    LLM Judge + 人工抽检
    
评分维度：
    事实正确性
    专业术语准确性
    逻辑完整性
```

##### Q14：为什么选择Qwen2.5-32B而不是7B、14B、72B？

```markdown
主要从效果、成本和部署三个维度考虑。
从32B到72B：参数增加2倍，效果只提升2%，收益明显递减。
```

##### Q16：Post-Train学习率怎么设置？

```markdown
Post-Train属于继续预训练，不是从头训练。
学习率过大会：覆盖原有知识（灾难性遗忘）

采用：Cosine Decay + Warmup
学习率：2e-5（通过实验所得）
Warmup：3%
```

##### Q17：怎么看训练发散？

```markdown
观察Loss曲线
监控：Gradient Norm
```

##### Q18：Post-Train阶段主要看哪些指标？

```markdown
1 Loss：训练是否正常。
2 Perplexity：语言建模能力。
3 通信专项评测：每隔500 step，跑专项Benchmark。
```

##### Q19：Checkpoint如何选择？

```markdown
Loss最低 ≠ 效果最好

every 1000 step 保存，并测：
    通信QA
    故障诊断
    工单分析
综合打分。
```

##### Q20：为什么做两阶段SFT？

```markdown
阶段1：通用指令能力。
训练：
    指令跟随
    格式遵循
    多轮对话
    
阶段2：专业能力。
训练：
    通信知识
    故障推理
    工单分析
    
如果直接混训：
模型容易：格式能力不足 或者 专业能力不足
```

##### Q21：SFT学习率为什么比Post-Train小？

```markdown
1e-5，实际上SFT更容易过拟合。
SFT数据量：几十万
远小于：Post-Train数十亿Token

学习率过大：快速记忆样本，泛化能力下降。
```

##### Q22：SFT训练多久停止？

```markdown
采用Early Stop。
监控：Validation Loss
```

##### Q23：DPO训练最重要的参数是什么？

```markdown
β（beta），DPO核心超参。
控制：偏好学习强度

| β    | 效果  |
| ---- | --- |
| 0.01 | 学不动 |
| 0.05 | 较好  |
| 0.1  | 最优  |
| 0.5  | 过拟合 |
```

##### Q24：DPO Loss下降是否代表效果提升？

```markdown
不一定。
经常出现：DPO Loss下降
但是：幻觉率上升

# 同步监控：
    Hallucination Rate
    Consistency
    Task Accuracy
```

##### Q25：Qwen2.5-32B训练为什么选择ZeRO-3？

```markdown
FP16下：32B × 2Byte ≈ 64GB

训练时还有：
Optimizer
Gradient
Activation

实际：>200GB
```



##### Q27：梯度累积怎么算？

```markdown
公式：
Global Batch = Micro Batch × GPU数 × Gradient Accumulation

例如：
Micro Batch = 2
GPU = 8
GA = 16
那么：
Global Batch=2×8×16=256
```

##### Q28：如何提升Token利用率？

```
很多训练资源浪费在Padding。

Packing，把多个短样本拼接。
50+100+200+300
凑满：2048窗口
```

##### Q29：如果老板只给你8张A800，你怎么训练Qwen2.5-32B？

```
QLoRA
+
ZeRO-3
+
Gradient Checkpoint
+
FlashAttention2
+
Sequence Packing
```

##### Q30：基于 Qwen3-32B 模型预训练，在128 张 A100 上训练，预训练数据为 15B 中英文数据（5%的32k长文本），epoch设置为3，最快训练需要多久？

```
最快约 10 天级别完成 pretraining，工程现实通常在 12–18 天之间。
```



##### Q31：如何评估预训练的效果？(⭐)

1、查看训练指标

| 现象                 | 含义                      |
| -------------------- | ------------------------- |
| train ↓, val ↓       | 正常学习                  |
| train ↓, val plateau | 数据或容量瓶颈            |
| train ↓, val ↑       | 过拟合 / 数据污染         |
| loss 振荡            | 学习率 / batch / 并行问题 |

2、评估领域知识正确性

通信知识QA，查看 Accuracy（单选/多选）、EM（Exact Match）、F1（结构化答案）

协议理解能力

3、RAG能力

RAG评估指标（Retrieval Accuracy、Faithfulness、Citation Accuracy）

4、工程问题解决能力

故障诊断能力、KPI分析能力、网络优化建议质量等

5、推理能力（通信特化）

Multi-step reasoning、Constraint reasoning（约束推理）、Counterfactual robustness（反事实鲁棒性）

6、业务可用性评估

人类专家评估、在线模拟、A/B系统测试（故障解决时间下降、工单解决准确率上升、工程师工作量下降）

7、设计专用通信评测数据集

通信知识集、KPI分析集、故障诊断集、运维建议集、信令流程集



##### Q32：通信数据“脏 + 噪声 + 模板化严重”，如何解决

```markdown
1）分层数据清洗
    rule-based filter（日志模板去重）
    MinHash / SimHash 去重
    heuristic过滤（低信息密度文本）

2）通信语料“结构化重写”

3）重采样
提升高价值数据权重：（数据分类打标 + 赋权，离线加权重采样（构造训练集））
    故障诊断日志 ↑↑
    KPI因果分析 ↑↑
    3GPP原文 ↓（降权）
```

##### Q33：loss下降但能力不提升，如何解决？

```markdown
1）分domain loss监控
    必须拆：
    Chinese telecom loss
    English spec loss
    KPI logs loss
    code/log structured loss
    👉 防止单域过拟合
    
2）增加“对比学习信号”（隐式）
    构造：
        正确故障分析 vs 错误分析
        
3）学习率 warmup （预热）拉长
    32B 通信数据：
        通常需要更长 warmup（2% → 5% tokens）
        防止 early collapse
```

##### Q34：长文本能力退化，如何解决？

```markdown
1）长样本“强化采样”（重复采样）
    不是5%，而是：
    token-level oversampling（不是 sample-level）
    👉 让32K token贡献 ≥ 15%

2）context packing优化
    避免：
    	多段无关拼接
    改成：
        KPI时间序列连续拼接
        单故障完整链路拼接
    
3）RoPE scaling校准
    常用：
        NTK-aware scaling
        YaRN（推荐）
        👉 防止32K attention collapse

4）long-short curriculum learning
    训练顺序：
    4K → 8K → 16K → 32K
```

##### Q36：长上下文能力是通过继续预训练自然获得的，还是单独做了 long-context adaptation（比如 RoPE scaling / NTK / YaRN）？

```markdown
工业界基本是“混合方案”，不是二选一
可以理解为三段式：
    模型结构层（必须）：扩展 position encoding 上限
    训练层（关键）：注入长依赖分布
    对齐层（最后）：让模型学会“正确用长上下文”

长上下文能力一般不是单靠 continued pretraining 自然获得的，而是一个“结构改造 + 数据课程学习 + 对齐训练”的组合过程。

因此工业界通常会先做 RoPE scaling / NTK 或 YaRN 这类 position interpolation 方法，把 attention 的频域稳定性扩展到目标 context length，然后再通过 curriculum learning 的方式逐步引入长序列数据，比如从 4K → 16K → 32K。

最后还会做专门的 long-context task alignment，比如 multi-document QA 或 needle retrieval，来避免“能看长文本但不会用”的问题。
```

##### Q37：在长上下文数据占比较高的情况下，你们是如何处理 short/long sequence 的训练冲突的？

```markdown
short/long sequence 的冲突 = 梯度贡献不均 + 计算效率不均 + 表示学习目标不一致
“分阶段训练 + batch-level mixture control（动态混合）”的组合方案

1、分阶段训练（2K → 4K → 8K → 16K → 32K）
2、Token-level mixture（控制short token/long token = 2:1）
3、Dynamic batching（按长度分桶，再按桶取batch）
4、Loss reweighting（降低long token的权重）
```

##### Q40：RoPE scaling / NTK / YaRN

```markdown
# 目前YaRN最常用
https://www.doubao.com/chat/38432200367245826
```



##### Q41：为什么要做 CPT（持续预训练）？RAG 不够用吗？

1. **Tokenization 碎裂问题**：通用大模型对通信领域专有名词（如 gNodeB、Option 3x、QCI_9、RRCReconfiguration）切词极碎。CPT 能够优化模型对通信专用 Token 的表征效率，**节省 Context 窗口，提升 Attention 集中度**。
2. **隐式逻辑推理（Parametric Reasoning）**：RAG 只能提供“上下文片段”，但**故障诊断和告警拓扑分析**需要模型理解通信协议栈的隐式因果链（如：PHY层 CRC 错误 -> MAC 层 HARQ 重传 -> RLC 状态报告 -> RRC 断连）。CPT 调整的是模型底层的概率分布和推理拓扑，让它“天然具备通信领域的直觉”。
3. **最终架构权衡**：我们采用了 **“CPT/SFT 建立基座能力 + RAG 提供实时/确定性数据”** 的双轮驱动模式，而不是非此即彼。



##### Q42：如何解决合成数据的“自举悖论”与“幻觉循环”？

1. **拒绝无约束自由生成**：不让 LLM“凭空造题”，而是**以真实的通信日志、设备 CLI 指令集、3GPP 标准 AST（语法树）为硬性约束（Anchor）**。LLM 只负责包装 Prompt 和改写表达，逻辑骨架来自真实系统。
2. **确定性代码/规则校验器（Deterministic Validators）**：
   - 对于故障诊断场景：写正则和 Python 脚本，自动校验生成数据中的 CLI 命令语法是否合法、参数范围是否超限。
   - 对于标书分析场景：校验提取的表格字段与源文件 JSON Schema 是否完全匹配。
3. **验证成本 < 生成成本**：利用大模型“生成难、验证易”的特性，采用“小模型生成候选 -> 规则过滤 -> 强模型/通信专家盲审”的漏斗机制。



##### Q43：DPO 对齐陷阱：垂直领域的“偏好”到底是什么？如何避免 Reward Hacking？

1. **惩罚“冗长与废话”**：在故障诊断场景，运维人员需要极高效率。我们将**步骤过长、包含无意义客套话、或者推理链条过长但没有给出明确 CLI 排查命令**的回答，直接标为 yrejected。
2. **高危动作负偏好**：通信场景最忌讳“误操作导致基站大面积断网”。如果 Rejected 样本中包含高危指令（如未带过滤条件的高负载查询指令或高危重启指令），其惩罚权重拉满。
3. **控制 DPO 长度偏置**：采用 **Length-Normalized DPO** 或在构建 (ychosen,yrejected)对时，**严格控制两者的长度差（Length Matching）**，消除模型通过“翻倍输出长度”来骗取 Reward 的行为。



##### Q44：QLoRA 在 CPT 和 DPO 上的表达上限折损

1. **承认局限性**：实验表明，在 **CPT（增量预训练）阶段**，QLoRA 由于基座 4-bit 冻结，吸收新知识（Knowledge Capacity）的上限确实明显弱于 Full-FT。

2. **分阶段工程拆解（关键思考）**：

   - **CPT 阶段**：放弃 QLoRA，采用 **DeepSpeed ZeRO-3 + Offload** 进行 **Full-FT 或高 Rank（如** r=256）的 LoRA**

     ，保证模型对通信领域新知识的“消化能力”。

   - **SFT / DPO 阶段**：此时模型已经具备领域知识，主要是学习指令格式与行为对齐，参数改动量小，此时全面切入 **QLoRA (**r=64, 全 Linear 层挂载)，将训练成本降低 70% 以上。

3. **精度损失补偿**：在 QLoRA 中使用 bf16 混合精度，且必须作用于所有 Linear 层（Q, K, V, O, Gate, Up, Down），否则领域能力的泛化会显著受损。



##### Q45：15% 的准确率提升和 7% 的幻觉下降是怎么防刷榜（Overfitting）的？

1. **防数据泄露（OOD 动态评测集）**：评测集**严禁**使用已有的规范文档。我们使用**最近 1 个月内发生的真实脱敏运维故障工单**和**最新的通信设备升级日志**作为测试集（此时模型训练数据尚未覆盖）。
2. **幻觉率的定量定义**：
   - 构建**通信实体/参数规则库**。如果模型生成的答案中，包含了不存在的参数名、错误的命令语法、或与提供上下文矛盾的推论，计入 1 次幻觉。
   - 幻觉率下降 7% 指的是：在 1000 条复杂故障诊断 Prompt 下，经规则库自动校验 + 专家抽样核验，产生“严重幻觉（高危错误）”的样本比例由 12% 降至 5%。
3. **线上数据闭环（真正的落地）**：
   - 在智能运维 Portal 中埋点，提供“采纳”、“修改后采纳”、“拒绝”三个按钮。
   - 工程师**“修改后采纳”的对比数据**（工程师改动前的模型输出 vs 工程师修改后的最终输出），被自动洗成最高质量的 DPO 偏好对（Rejected vs Chosen），反哺下一代模型训练。



##### Q46：扩充词表到底是“救命药”还是“毒药”？（⭐）

1. **决策权衡（To Expand or Not）**：
   - 扩充词表可以提升 20%~30% 的文本压缩率（提升推理吞吐，降低上下文长度），但代价是**打破了原模型精心训练的 Embedding 空间**，需要大量的 CPT 语料（至少百 GB 级）才能重新将新 Token 训练收敛，否则新 Token 会导致模型产生严重语法混乱。
2. **工程解决方案**：
   - **如果不扩词表**：在 CPT 阶段，针对通信专有名词进行 **Subword 拼接训练**，利用 Sample Packing 缓解 Padding 浪费，靠算力硬抗。
   - **如果扩词表**：只扩充高频通信词（如 3000~5000 个），新 Token 的 Embedding 采用 **原词表中对应 Subword 的 Embedding 平均值（Mean Initialization）** 进行初始化，并在 CPT 早期**冻结除 Embedding 和 Head 以外的参数**（Warmup 阶段），先单训新词表，再做全量/LoRA 增量训练。



##### Q47：智能运维与招投标分析抢占参数空间怎么办？（⭐）

1. **梯度冲突现象**：招投标任务鼓励模型输出“详尽、多角度的自然语言”，而运维诊断要求“极简、高精准度的逻辑/指令”。直接混合训练会导致 Loss 震荡，诊断准确率下降。
2. **解题手段一：任务概率采样与分阶段 SFT**：
   - 采用 **Temperature-based Dynamic Sampling** 调节不同任务的采样权重。
   - 先用大比例的“通用问答+招投标长文本”训练语言基础能力，再用高质量的“故障诊断结构化数据”进行收尾微调（Annealing 阶段）。
3. **解题手段二：Multi-LoRA / MoE 架构解耦（高阶架构选型）**：
   - 线上部署时，底座模型统一，但针对“招投标”和“智能运维”挂载不同的 **LoRA Adapter**，在前置系统（Router）根据 User Intent 动态路由加载对应的 Adapter，从物理上隔绝参数干扰。



##### Q49：生产环境部署的“最后一公里”：PTQ 量化导致的通信指令精度崩塌

“你们训练用 QLoRA，最后部署时肯定要合并权重（Merge Adapter）并用 vLLM / TensorRT-LLM 部署。**为了追求高并发，通常会对 Merge 后的模型做 AWQ / GPTQ 4-bit/8-bit 量化。但量化往往会导致精确语法（如网管 CLI 命令）出现字符错乱**，你们遇到了吗？如何解决生产部署的精度崩溃？”

1. **混合精度量化（Selective Quantization）**：
   - AWQ/GPTQ 全量化会导致某些对字符极其敏感的层（如 LM Head 和部分关键 Transformer 层）精度受损。
   - 通过计算激活值敏感度（Activation Sensitivity），**将 LM Head 和关键层保留为 FP16 / FP8，仅对其余层做 INT4/INT8 量化**。
2. **解码端硬约束（Grammar-Guided Decoding / GBNF）**：
   - 模型推理层面，在 vLLM 后端挂载 **Outlines 或 GBNF (GGML Backus-Naur Form) 语法树**。
   - 当模型生成网管 CLI 命令或 JSON 诊断结果时，强制在 Logits 采样阶段 Mask 掉不符合通信语法规范的 Token，保证 100% 输出合法命令，将“量化导致的语法错误”在推理解码阶段直接拦截。



##### Q50：通信领域“小语料”CPT 的“数据饱和”与“过拟合/幻觉爆炸”博弈

1. **数据重复（Epoch > 2）的毒性**：领域小语料如果重复训练超过 2 个 Epoch，模型会迅速将文本“死记硬背”，表现为 Loss 极低，但遇到微小的参数扰动（如更改网络拓扑中的 IP 地址）就会死板输出固定的错误日志片段。
2. **训练策略**：
   - **严格限制 Epoch** ≤1.5：绝不为了降 Loss 而在小语料上过度循环。
   - **动态学习率与数据交织**：采用带有衰减控制的 Learning Rate（如从 5e−5 线性衰减至 5e−6），并在 CPT 中交织 **15% 的通用高质量代码与逻辑推理语料**（如 OpenHermes、Code Llama 采样）。
3. **Stop Signal（停止信号）**：设置 Validation Set 监控两类指标——当通信领域验证集 PPL 降幅小于 1% **且** 通用逻辑推理 benchmark（如 MMLU/GSM8K 子集）准确率下降超过 2% 时，立即触发 Early Stopping。



##### Q51：增量对齐与数据飞轮中的“流式更新灾难”（Incremental SFT/DPO Collapse）

1. **经验回放缓冲池（Replay Buffer）机制**：
   - 建立一个固定规模（如 10,000 条）的 **Anchor 数据集**，包含历史训练集中最具代表性的各子任务数据（故障诊断、标书提取、规范问答）及基座通用能力数据。
   - **数据混合微调**：当新增 500 条 Badcase 数据时，并不单训这 500 条，而是从 Replay Buffer 中按 1:4 的比例抽样历史老数据（2000 条），组成 2500 条的增量 Batch 进行微调。
2. **LoRA 级别的物理隔离**：
   - 如果新增数据属于全新的子业务（例如新增了“5G RedCap 专项网管”），则**不修改原有 LoRA 参数**，而是训练一个新的子 Adapter，利用 Router 在推理时进行动态加载，避免污染主模型。



##### Q52：训练中途突然出现loss峰值，如何排查？



##### Q52：显存与硬件瓶颈问题：显存溢出（OOM - Out of Memory）

- **现象**：训练刚开始或跑了几步后突然报 CUDA out of memory。

- **解决办法**：

  - **参数高效微调（PEFT/LoRA）**：改用 **LoRA / QLoRA**（4-bit 量化微调），可将显存需求降低 60%~80%。

  - **梯度累积（Gradient Accumulation）**：减小单卡 Batch Size（如设为 1 或 2），通过梯度累积达到目标 Total Batch Size。

  - **梯度检查点（Gradient Checkpointing）**：以计算时间换显存，大幅降低激活值（Activation）占用的显存。

  - **混合精度训练**：使用 bf16（首选，硬件支持 Ampere 及以上架构）或 fp16。

  - **分布式显存优化**：使用 **DeepSpeed (ZeRO-2 / ZeRO-3)** 或 PyTorch **FSDP**，将模型状态分片到多张 GPU 上。

  - **上下文长度裁剪**：适当缩短最大序列长度（max_length），因为 self-attention 的显存开销随长度呈二次方O(N2)增长。可配合 **FlashAttention-2/3** 降低显存开销。

    

##### Q53. 训练速度极慢

- **现象**：GPU 利用率（GPU-Util）低、数据加载瓶颈，或每秒处理 Token 数太少。
- **解决办法**：
  - 开启 **FlashAttention-2** 或 SDPA（Scaled Dot-Product Attention）。
  - 优化 DataLoader：增加 num_workers，开启 pin_memory=True。
  - 使用高效微调框架：如 **Unsloth**（可提升 2-5 倍训练速度并节省显存）、**LLaMA-Factory**、**Deepspeed**。

##### 一、 训练过程与 Loss 异常问题

###### Loss 变为 NaN 或 Inf（梯度爆炸/数值溢出）

- **现象**：训练几百步后 Loss 突然变成 NaN，训练崩溃。
- **原因**：FP16 动态范围较小容易上溢，或学习率过大、数据中存在异常值。
- **解决办法**：
  - **改用 bf16**：BF16 的数值范围与 FP32 一致，极少发生溢出。
  - **梯度裁剪（Gradient Clipping）**：设置 max_grad_norm = 1.0 或更小（如 0.5）。
  - **调小学习率（Learning Rate）**：LLM 微调的学习率通常非常小（全量微调：1e−5∼5e−6；LoRA：1e−4∼3e−4）。
  - **清洗数据**：排查数据集中是否存在极长文本、空文本或非法字符。

###### Loss 不下降 / 无法收敛

- **现象**：训练多个 Epoch 后，Loss 居高不下或平躺。

- **解决办法**：

  - **排查 Prompt 模板**：检查训练数据格式是否与基座模型的 Chat Template 一致（如 <|im_start|> 等特殊 Token 是否正确插入）。

  - **增加 Warmup 步数**：加入 3%~5% 的 Learning Rate Warmup。

  - **检查 Loss Masking**：确保计算 Loss 时**只对 Model Response 计算，Mask 掉 User Instruction**（即 train_on_inputs=False）。如果对 Prompt 也计算 Loss，模型容易学偏。

  - **增加 LoRA 的 Rank (**r) 和 Alpha**：如果用了 LoRA，可能是可训练参数太少，可以尝试将 r 从 8 提高到 64 或 128。

    

###### Loss 突然飙升（Loss Spike）

- **现象**：Loss 本来很平滑，突然产生一个巨大的尖峰，然后又慢慢下降或直接崩掉。

- **解决办法**：

  - 调低学习率，增加 Warmup。

  - 增大 Batch Size（或增加梯度累积步数），使梯度估计更稳定。

  - 调整 AdamW 优化器参数：例如将 β2 从 0.999 改为 0.95 或 0.98。

    

------



##### 二、 模型效果与泛化能力问题

###### 灾难性遗忘（Catastrophic Forgetting）

- **现象**：微调后，模型在特定领域变强了，但失去了通用对话、逻辑推理或代码能力，甚至连基本的问答格式都乱了。
- **解决办法**：
  - **数据混采（Data Replay / Mixing）**：在领域数据中混入 5%~10% 的通用高质量数据（如 ShareGPT、Alpaca 或 SlimPajama）。
  - **优先选择 LoRA 微调**：LoRA 保留了冻结的基座权重，相较于全量微调更容易保留基座模型的通用能力。
  - **使用权重衰减（Weight Decay）和较低的 Epoch 轮数**：微调数据量不大时，通常 1~3 个 Epoch 足矣，避免多轮训练。

###### 模型过拟合（Overfitting）与死记

- **现象**：训练集的 Loss 降得非常低，但测试时稍微改变问法，模型就无法正确回答，甚至直接复读训练集原句。
- **解决办法**：
  - **提高数据多样性**：扩充同一指令的不同表达方式（Prompt 多样化）。
  - **减少训练 Epoch**：很多情况下，微调数据集跑 **1~2 个 Epoch** 效果最好。
  - **早停法（Early Stopping）**：监控 Validation Loss，一旦验证集 Loss 开始上升立即停止。

###### 复读机现象 / 幻觉严重 / 乱码输出

- **现象**：模型不断重复上一句话，或者输出不符合逻辑的胡言乱语。
- **解决办法**：
  - **检查 EOS Token（结束符）**：确保训练数据结尾正确添加了模型对应的 <|im_end|> 或 </s>，否则模型不知道何时停止，容易狂吐重复字符。
  - **推理采样参数调整**：微调后需要重新调整推理参数（Temperature 降至 0.1~0.7，开启 Repetition Penalty，设置 top_p 等）。
  - **清洗低质数据**：检查训练集中是否有大量的长文本重复或质量极差的 LLM 生成数据。

------



##### 三、 数据与格式问题

###### Special Tokens 错误处理

- **现象**：模型把控制标记（如 <|im_start|>）当成普通文本打印出来，或者无法识别对话轮次。
- **解决办法**：
  - 使用官方提供的 Tokenizer 和 apply_chat_template 方法。
  - 确保自定义的新 Token（如有）已被成功添加至 Tokenizer 且词表已被扩展（Resize Token Embeddings）。

###### 数据质量差、噪声多

- **现象**：微调后模型回答生硬、含有错误事实或模仿了数据里的低级错误。
- **解决办法**：
  - **高质量胜过大数量**（Quality > Quantity）：1,000 条高质量、经人工精细校对的 SFT 数据，效果远好于 10 万条爬虫抓取的噪声数据。
  - **去除重复数据**：对 SFT 数据进行 Deduplication（去重）。



### E. 移动通信垂域轻量化大模型多阶段知识蒸馏与落地 - 后训练（⭐）

#### 简历版

```

```



#### 模拟面试

```markdown
**项目名称**：面向通信运维与客服场景的垂直大模型蒸馏与压缩
**技术栈**： PyTorch, Transformers、TRL, DeepSpeed (ZeRO-3), FlashAttention-2, MiniLLM / Sequence-level KD, AWQ, vLLM
**项目描述**：
	通信网络运维（网优、基站告警排查）与高并发智能客服场景对响应延迟、吞吐量和私有化部署成本有严苛要求。原系统采用 Qwen2.5-72B 级别的大模型，推理成本极高且吞吐量无法满足百万级日请求并发需求。为此主导了知识蒸馏项目，旨在将 72B 教师模型的通信专业知识与复杂推理能力（CoT）无损迁移至 7B 学生模型中，实现端/边/私有云低成本快速部署。
**核心工作**：
1.高质量通信垂域蒸馏数据集构建（Data Distillation）
	从海量通信协议（3GPP、5G Core）、工单排障日志及计费客服对话中筛选出 150 万条原始数据，构建质量过滤与去重 Pipeline（利用 MinHash + 规则清洗）。
	采用 Sequence-level KD 策略，使用 70B 教师模型针对网络拓扑排障等复杂场景生成带有思维链（Chain-of-Thought）的高质量解题过程，并通过自我一致性（Self-Consistency）与启发式规则过滤出 50 万条高质量指令遵循样本。

2.多阶段知识蒸馏架构设计（Multi-stage Distillation）
    Logits 级软标签蒸馏（Token-level KD）：设计带温度系数（Temperature Scaling）的 KL 散度损失，同时结合交叉熵损失（Cross-Entropy Loss），在预训练后续阶段（Continual Pre-training）对齐学生模型与教师模型在通信专有名词及拓扑推理上的输出分布。
    中间隐藏层与注意力对齐（Feature-based KD）：针对关键 Transformer 层设计 MSE 损失与注意力矩阵相似度损失，解决深浅层结构不匹配问题，加速学生模型表征收敛。
    生成分布偏好对齐：引入 GKD（Generalized Knowledge Distillation）与 DPO 机制，使用教师模型作为奖励打分器，解决自回归生成中的 Exposure Bias（暴露偏差）问题，显著降低幻觉率。

3.分布式训练与工程加速优化
	基于 DeepSpeed ZeRO-3 + FlashAttention-2 构建高效蒸馏分布式流水线，在 4 节点 × 8 卡 A800/H800 环境下，实现显存占用降低 40%，整体蒸馏吞吐提升 2.3 倍。
	针对蒸馏过程中教师模型 Forward 计算开销过大的问题，设计了 Offline Logits Cache 与流式数据加载机制，将训练耗时缩短 35%。

4.量化与部署优化（PTQ & Inference）
	蒸馏完成后，采用 AWQ（Activation-aware Weight Quantization） 对学生模型进行 4-bit 权重量化，显存占用降低至 4.8GB（FP16 → INT4）。
	基于 vLLM 进行部署，启用 PagedAttention 与连续批处理（Continuous Batching）。
```



#### 模拟问题



------



# 7.2 比赛经历

## 2026-FlagOS开放计算全球大赛

### 简历版

```markdown
# 赛题：
[长上下文场景中大模型自动数据标注](https://flagos.io/RaceDetail?id=296fmsd8&lang=cn)（三等奖）
# 项目描述：
本次挑战赛旨在**探索大语言模型在超长上下文（如数万至数十万token）范式下的数据标注能力提升方法**，重点评估模型在复杂、长序列标注场景中的稳定性、泛化性与实用性。
# 核心工作：
• 基于Qwen3-4B模型与ICL范式，优化Nanobot智能体运行架构，设计自适应少样本遴选策略，解决长上下文场景资源浪费、标注精度偏低等问题；引入轻量化GRPO强化学习方案，零参数更新优化模型判别能力。
• 覆盖情绪检测、主题分类、百科问答、算子代码生成多类任务，搭建代码闭环核验体系与多线程标注框架，多维度优化算法策略并完成消融实验，沉淀可落地的长文本数据标注解决方案。
# 技术难点：
1. 在超长上下文场景下，如何设计有效的模型指令与提示策略，引导 LLM 稳定、高质量地完成数据标注任务？
2. 当可用标注示例数量显著超过模型上下文容量时，如何为待标注数据构造信息密集、结构合理的超长上下文输入？
3. 在自动多轮对话或持续交互场景中，如何高效利用超长上下文，实现一致性与可扩展性兼顾的数据标注？
```



------

#### 面试中的一句话总结

> 这个项目本质上是在长上下文自动标注场景下，构建了一套“Context Engineering + Prompt Optimization + RL-based Boundary Learning”的闭环优化系统。通过动态上下文组织提升信息利用率，通过NanoBot实现Prompt自动优化，再利用Multi-class GRPO从误分类样本中学习类别边界知识并回灌Prompt，最终实现自动标注能力的持续迭代与自进化。

## **2023-全国先进计算技术创新大赛**

### 简历版

```markdown
# 赛题：
[2023D3-基于计算硬件平台的AI大模型迁移](http://www.ac-innovation.com/#/home)（一等奖）
# 项目描述：
基于曙光计算服务平台为基础，进行大模型迁移或研发。在研发过程中需基于曙光自研的软件栈（配套有成熟软件库、编译器等），也使用的其他相关软件实现。
# 核心工作：
1、基于国产ChatGLM2-6B中文大模型，完成了在Slurm调度系统+DCU硬件环境下的环境适配，并进行P-tuning v2调优，模型整体性能达赛道SOTA级别。
2、在推理方面，模型性能在验证数据集为0.27，在测试数据集为0.70，相比于SOTA（ChatGLM2-6B,0.50）模型存在一定差距，通过代码初步分析了相关原因。
3、完成多场景数据集推理评测，量化分析模型推理指标偏差原因，沉淀国产硬件下大模型部署、调优实战经验。
```



### 模拟面试

```markdown

```

# 7.3 自我介绍

面试官您好，我叫肖傲文，重庆大学电子与通信工程硕士，拥有近 4 年大模型与算法研发经验，今天应聘 “大模型算法工程师” 岗位。

我先后在浪潮通信、马上消费金融任职，覆盖大模型训练、部署及应用的多条链路技术栈。

在浪潮期间，我主导了大模型异构算力训推平台建设，完成 GPU、昇腾 NPU、海光 DCU 多类硬件的模型适配；搭建了通信领域大模型从持续预训练、SFT 到 DPO 对齐的完整训练流水线，实现业务场景问答准确率提升、幻觉率下降；同时参与了企业内部智能体平台的 RAG 架构设计，降低幻觉率，缩减评估人力成本。

加入马上消费后，我聚焦 Agent 自进化与零幻觉推理方向，落地了对话数据标注自进化 Agent 系统，通过非参数 GRPO 优化、多 Agent 动静隔离架构，实现核心业务分类与意图识别准确率平均提升 8% 左右；同时研发五阶段零幻觉推理框架，配套自动化幻觉评测体系，有效降低对话系统的大模型幻觉率。

此外我拥有两项行业赛事获奖经历，熟悉从模型训练对齐、推理部署到 RAG、Agent 系统落地的全流程研发，也具备昇腾和海光算力适配经验，希望能加入团队贡献价值，谢谢。



# 7.4 反问

> 我想了解一下，如果我加入团队，未来主要负责哪些方向的工作？目前团队希望解决的核心问题是什么？

> 从团队角度来看，目前这个岗位更希望候选人在技术深度还是项目落地能力方面有所贡献？

> 我了解到公司在大模型方向有布局，我比较关注技术预研，不知道团队目前是否会安排一些探索性项目？



自chatgpt发布之后，大模型后训练微调技术或算法的发展历程是怎么样的

我想找大模型后训练算法工程师的工作，帮我模拟3份项目经历，可以直接写进简历中的内容，要求：1、按照微调技术发展时间表模拟，较早的项目，用到的技术相对简单；2、有一段蒸馏训练（蒸馏大模型，训练小模型）的项目经历；3、时间为2022年 - 2025年10月份；4、工作内容聚焦于三大运营商（移动、联通、电信）

