# 一、基础

## Transformer架构





## 主流开源大模型



### MoE

### Scaling Law



## 多模态大模型



## 世界大模型





## 模拟面试

### Transformer

#### Q1：请你整体介绍一下 Transformer 架构，以及 Self-Attention 的核心作用

```markdown
Transformer 主要由 Encoder 和 Decoder 组成（原始论文结构），每一层包括 Multi-Head Self-Attention 和 Feed Forward Network。

Self-Attention 的核心是通过 Q、K、V 计算序列中每个 token 与其他 token 的相关性，从而实现全局依赖建模。它解决了 RNN 难以并行计算以及长距离依赖难以捕捉的问题。
```

#### Q2：为什么 Self-Attention 可以解决“长距离依赖”，RNN 的瓶颈本质是什么？

```markdown
RNN 是链式结构，信息需要逐步传递，路径长度是 O(n)，导致远距离 token 梯度容易消失。

Self-Attention 中任意两个 token 直接计算相关性，路径长度是 O(1)，因此更容易建模长距离依赖。
```

#### Q3：Self-Attention 计算复杂度是 O(n²)，为什么工业界还能用？

```markdown
虽然计算复杂度是 O(n²)，但 Transformer 支持高度并行化，GPU 上效率高。

同时在工程上可以通过：
    sparse attention
    sliding window attention
    FlashAttention
    等方式优化。
```

#### Q4：你提到了 FlashAttention，它具体优化了什么瓶颈？

```
FlashAttention 主要优化的是 显存 IO（HBM 访问），不是数学复杂度。

通过 tile 分块计算 attention，避免显式存储完整 attention matrix，从而减少显存读写，提高速度。
```

#### Q5：如果不用 FlashAttention，标准 attention 的 memory bottleneck （内存瓶颈）在哪里？

```markdown
主要瓶颈是：
    Attention matrix 是 n×n
    需要存储 softmax(QK^T)
    反向传播还要保存中间结果

所以显存占用是 O(n²)，长序列会直接爆显存。
```

#### Q6：Transformer 没有 RNN 的顺序结构，那它是怎么建模 token 顺序的？

```markdown
通过 Positional Encoding（位置编码）注入位置信息。

原始 Transformer 使用 sin/cos 位置编码，也可以学习 embedding。
```

#### Q7：有哪些位置编码？

```markdown

```

#### Q8：为什么 sin/cos 位置编码是“可外推”的？相比 learned embedding 优势是什么？

```markdown
sin/cos 是函数形式，可以泛化到训练长度之外的 position。

而 learned embedding 只能覆盖训练时见过的位置，超长序列会失效。
```

#### Q9：现在 LLM 为什么很多不用 absolute positional encoding，而用 RoPE？

```markdown
RoPE（Rotary Position Embedding）是相对位置编码的一种实现方式。

它的优势：
    将 position 以旋转方式作用在 QK 上
    attention score 天然包含相对距离
    更适合长上下文建模
```

#### Q10：RoPE 在长上下文外推时会有什么问题？

```markdown
会出现 频率外推失真（extrapolation degradation），因为旋转角度线性增长后超出训练分布。

因此会有：
    NTK scaling
    YaRN
    等方法做修正。
```

#### Q11：推理阶段 Transformer 最大的工程优化点是什么？为什么？

```markdown
主要是 KV Cache。
因为自回归生成时：
    K、V 可以复用历史 token
    不需要重复计算
    将复杂度从 O(n²) 降为 O(n)
```

#### Q12：KV Cache 可以优化推理，那为什么不能用于训练？

```markdown
KV Cache 是在自回归推理时缓存历史 K/V，避免重复计算。

训练阶段是并行计算整个序列，每个 token 都要 attend 所有 token，所以无法复用历史 cache。
```

#### Q13：训练也是可以 cache 的，你知道哪里吗？

```markdown
训练时：
	forward 是全序列 attention
	但 decoder-only 模型在 causal mask 下是可以做 block-wise KV reuse 的（如 FlashAttention2 / sequence packing）

但问题是：
    反向传播需要完整 attention graph
    KV cache 会破坏 computation graph 的完整性
```

#### Q14：那为什么 inference 可以 cache，training 不行？本质差异到底是什么？（+1）

```markdown
Training：需要梯度传播 + 完整 attention matrix
Inference：只是 forward sampling

KV Cache 本质：
	用空间换时间，但破坏了反向传播依赖结构
```

#### Q15：你说 attention 是 QK^T / sqrt(d)，为什么一定要除 sqrt(d)？

```markdown
防止 dot product 结果过大导致 softmax saturation。
```

#### Q16：如果不用 scaling，会发生什么“数学级别”的问题？

```markdown
如果不缩放：
QK^T 方差随 d 增大
softmax 输入进入极端区间
导致：
    梯度接近 0
    attention 变成 one-hot
```

#### Q17：那为什么 scaling 用的是 √d，而不是 d 或 log d？

```markdown

```

#### Q18：Transformer 为什么一定要 Multi-Head？单头不行吗？

```markdown
Multi-head 可以让模型在不同子空间学习不同关系。
本质区别：
    Multi-head = 多个低维 attention subspace
    Single-head = 一个高维 attention space
```

#### Q19：那如果我把 single-head dimension 调到 multi-head 总维度一样，理论上表达能力是否等价？

```markdown
理论上表达能力接近，但优化性质不同：
Multi-head 优势在：
    更容易优化（inductive bias）
    梯度更稳定
    不同 head 专注不同 pattern
```

#### Q20：Transformer 为什么需要 residual connection？去掉会怎样？

```markdown
避免梯度消失，使训练稳定。
因为深层 MLP + attention stack：

深层矩阵乘法链
softmax + linear projection
Jacobian 连乘

仍然可能：
gradient norm exponential decay
```

#### Q21：Residual connection 本质上解决的是什么数学问题？

```markdown
本质是：

让网络学习 residual function：
F(x) = x + g(x)

等价于优化：
    identity mapping easier to preserve
    reduces function space curvature
    improves conditioning of optimization landscape
```

#### Q22：正向传播推导

设输入矩阵为 
$$
X∈R^{N×d}
$$
其中 N 为序列长度（Sequence Length），d 为特征维度（Embedding Dimension）。

定义以下权重矩阵：

- 查询投影矩阵：
  $$
  W_Q∈R^{d×d_k}
  $$

- 键投影矩阵：
  $$
  W_K∈R^{d×d_k}
  $$

- 值投影矩阵：
  $$
  W_V∈R^{d×d_k}
  $$

- 输出投影矩阵：
  $$
  W_O∈R^{d×d_k}
  $$
  

自注意力机制的正向传播可以分为以下五个步骤：

##### Step 1: 线性投影（Linear Projection）

$$
Q=XW_Q, K=XW_K ,V=XW_V
$$

##### Step 2: 计算注意力得分（Attention Scores）

计算 Q 和 K 的内积，并进行缩放：
$$
S=QK^T/\sqrt[]{d}
$$

##### Step 3: Softmax 归一化（Attention Weights）

对得分矩阵的每一行进行 Softmax 归一化，得到概率分布矩阵 P：
$$
P=softmax(S)
$$
其中，对于矩阵的第 i 行、第 j 列：
$$
P_{ij}=e^{S_{ij}}/∑_{m=1}^Ne^{S_{im}}
$$

##### Step 4: 加权求和（Context Vector）

利用权重矩阵 P 对 V 进行加权求和，得到自注意力的直接输出 O：
$$
O=PV
$$

##### Step 5: 输出线性变换（Output Projection）

经过最后的线性层映射回原始维度：
$$
Y=OW_O
$$


#### Q22：手撕 Transformer 反向传播推导（核心版）

##### Step 1：Attention forward

$$
Q=XW_Q, K=XW_K ,V=XW_V
$$

$$
A=softmax(QK^T/\sqrt[]{d})
$$

$$
O=AV
$$

Step 2：定义 loss



Step 3：从输出开始反传



Step 4：softmax 求导（关键难点）



Step 5：对 Z 求梯度



Step 6：对 Q 和 K



Step 7：回到 W_Q, W_K, W_V



Transformer 反向传播核心是：

> “softmax Jacobian + QK bilinear form + matrix chain rule”

#### Q23：Self-Attention直观理解

```markdown
Self-Attention（自注意力）就是让序列里的每一个词，都能“环顾四周”，根据上下文的重要性，动态地从所有其他词那里“抽取”信息来丰富自己，从而生成更深层的语义表示。
```

#### Q24：Transformer整体结构

```markdown
Transformer 本质上是一个基于编码器-解码器（Encoder-Decoder）架构的序列模型，但它彻底抛弃了传统的 RNN/CNN，完全依赖注意力机制来捕捉全局依赖关系。整个结构可以看作一个沙漏型：左边是编码器，右边是解码器，中间通过交叉注意力相连。

1. 输入端（模型的“五官”）
	输入先经过 Embedding 层转为向量。
	关键点：由于 Self-Attention 没有顺序感（它是置换不变的），必须加上 位置编码（Positional Encoding）。原论文用的正弦余弦，现在很多大模型用可学习的位置编码或 RoPE（旋转位置编码）。

2. 编码器（Encoder，负责“阅读理解”）
它由 N 个（原论文 6 个）完全相同的层堆叠而成，每一层有两个核心子层：
	子层1（多头自注意力）：每个词看全局，动态聚合上下文信息（这就是你刚才问的 Self-Attention 的具体落地）。
	子层2（前馈神经网络 FFN）：一个两层的全连接网络（先升维再降维），负责对每个位置的向量做非线性变换，提炼特征。
	关键设计：每个子层外面都套了残差连接（Add）和层归一化（Norm）。顺序是 先 Attention/FFN，再 Add，再 Norm（即 Post-Norm）或现在流行的 Pre-Norm（先 Norm 再操作，训练更稳）。

3. 解码器（Decoder，负责“逐词翻译/生成”）
它也由 N 个层堆叠，但每一层比编码器多了一个子层（共3个），且做了遮罩：
	子层1（带掩码的自注意力）：和编码器一样，但加了 Mask（掩码）。因为在生成第 t 个词时，不能偷看未来的词（即 t+1, t+2...），所以要遮住右边未生成的部分。
	子层2（交叉注意力 / Encoder-Decoder Attention）：这是连接编码器和解码器的桥梁。它的 Q（查询）来自解码器上一个子层的输出，而 K（键）和 V（值）来自编码器的最终输出。直观理解就是：解码器每生成一个词，都要去编码器那边“查字典”，看源文本中哪些词最相关。
	子层3（前馈神经网络 FFN）：同上，做特征强化。

4. 输出端（模型的“嘴巴”）
解码器最后输出的向量，经过一个 线性层（Linear） 映射到词表大小，再套一个 Softmax 输出概率分布，挑概率最高的词作为预测结果。
```

#### Q25：Post-Norm 和  Pre-Norm 有什么区别？（+1）

```markdown
第一步：一句话定差异（面试官第一印象）
Post-Norm 和 Pre-Norm 的核心区别在于层归一化（Layer Norm）在残差块中的位置不同。
Post-Norm（原始Transformer）：先做子层操作，再做归一化。公式是 Norm(x + SubLayer(x))。
Pre-Norm（现代主流）：先做归一化，再做子层操作。公式是 x + SubLayer(Norm(x))。
别看只是调换了顺序，这直接导致了深层网络训练难度的天壤之别。

第二步：用公式和“梯度流”讲透本质（核心技术点）
1. Post-Norm（原始设计，理想很丰满）
公式：输出 = LayerNorm( x + SubLayer(x) )
致命问题（为什么难训）：因为 LayerNorm 是整个残差块的“最后一道门”。在反向传播时，梯度必须穿过 LayerNorm 层才能流回 SubLayer。
后果：LayerNorm 会对输入进行缩放和平移，导致靠近输出层的梯度在回传时被大幅压缩或扭曲。尤其在深层（比如 12 层以上），浅层（靠近输入层）的梯度极易爆炸或消失。所以原始 Transformer 必须配合学习率 Warmup（先小后大）来勉强稳定训练——Warmup 本质上就是在等 Post-Norm 的梯度方差逐渐稳定下来。

2. Pre-Norm（现代改良，现实很骨感）
公式：输出 = x + SubLayer( Norm(x) )
核心优势（为什么稳）：残差连接 x + ... 把 LayerNorm “隔离”在了子层内部。反向传播时，梯度走两条高速公路：
路径一（捷径）：梯度直接通过 x 恒等映射回传，完全不经过任何归一化层，完好无损地传到浅层。
路径二（子层）：梯度才需要穿过 Norm 和 SubLayer。
结论：总有“一条梯度高速公路”是绝对通畅的。这就保证了即使堆叠上千层，浅层也能收到高质量的信号，无需 Warmup 也能稳定训练。

第三步：权衡取舍（展现辩证思维，面试官最爱听）
既然 Pre-Norm 训练这么稳，为什么原始论文还用 Post-Norm？因为这里存在一个‘稳定性 vs 性能上限’的经典权衡：
Pre-Norm 的代价：由于输入先被 Norm 强行拉回均值为0、方差为1，这相当于削弱了残差连接中‘原始输入 x’的强度。模型的表示能力（Representation Capacity）会有轻微下降——通俗说，为了好训练，牺牲了一点点上限潜力。
Post-Norm 的优势：Norm 在最后，允许残差支路的原始信息尽可能多地保留和叠加，理论上模型容量的天花板更高（如果训得动的话）。

第四步：落到工程现实（终极加分项）
在实际工业界和前沿大模型中，Pre-Norm 是绝对的主流。
比如 GPT-3、PaLM、LLaMA 全系列，清一色用的 Pre-Norm（具体多是 RMSNorm，它是 LayerNorm 的简化变体）。因为在大规模训练中，训练的稳定性 >> 理论上的那点容量损失。况且，损失的上限可以通过增大模型宽度或数据量来弥补，但训练崩了（Loss 爆炸）就什么都没了。

第五步（预判追问）：如果面试官追问“那现在前沿在用什么？”
现在很多新模型（比如 LLaMA 2/3）在 Pre-Norm 基础上，把 LayerNorm 换成了 RMSNorm（去掉了均值中心化，只做缩放）。因为它计算量更小，且在 Pre-Norm 结构下，中心化带来的好处微乎其微，省下的算力可以拿去堆更多数据。
```

#### Q26：为什么是层归一化，而不是Batch归一化？（+1）

```markdown
第一步：一句话定调（直接亮明观点）
在Transformer和绝大多数NLP模型中，必须使用层归一化（Layer Norm），而不能使用批归一化（Batch Norm）。这并非偏好问题，而是因为NLP数据的特性与Batch Norm的设计初衷存在根本性的冲突。

第二步：极简数学维度对比（展现基本功）
Batch Norm：在批次（Batch）维度上做归一化。它统计的是同一个Batch中，不同样本的同一位置特征的均值和方差。
Layer Norm：在特征（Feature）维度上做归一化。它统计的是同一个样本内部，跨所有特征维度的均值和方差。
（结论）：BN 依赖“样本间”的关系，LN 依赖“样本内”自身的关系。

第三步：细数 Batch Norm 在 NLP 中的“三大死穴”（核心干货）
这部分要讲出层次感，为什么 BN 在 Transformer 里完全行不通：
1. 序列长度可变（变长序列灾难）
NLP 的输入长度是不固定的（比如短句5个词，长句50个词）。BN 需要在 Batch 维度上统计同一个“位置”的统计量。
致命伤：第50个位置在短句中是 Padding（填充符），在长句中才是真实语义。如果把 Padding 和真实词混在一起算均值和方差，会引入大量噪声，导致训练崩塌。LN 只看单个样本内部的词，完美避开跨样本对齐位置的难题。

2. 训练与推理的不一致性（Online 与 Global 的冲突）
BN 在训练时用当前 Batch 的均值和方差，在推理时却要用全局滑动平均（历史所有 Batch 的统计值）。
致命伤：NLP 的分布极其灵活（例如测试集突然全是科技新闻，而训练集包含小说）。一旦推理时的全局统计量无法覆盖当前输入的特征分布，模型表现会急剧下降。LN 没有全局统计量，它永远只算当前输入自己的均值和方差，训练和推理完全一致，极其鲁棒。

3. 自回归生成时的“未来信息”泄露风险（针对 Decoder）
在 GPT 等自回归模型中，生成第 t 个词时，不能看到t+1 个词。
致命伤：如果强行用 BN，它会依赖 Batch 中其他样本的统计信息，这在分布式训练（数据并行）下，变相让当前样本窥探到了其他样本的全局分布，破坏了“样本独立性”假设。LN 每个样本独立计算，天然符合独立同分布要求。

第四步：反向追问（如果你是面试官，会觉得这个候选人极其扎实）
反过来想，为什么 BN 在 CV（计算机视觉）中是王者，在 NLP 中却成了青铜？
自答：因为图像（CV）的输入尺寸通常是固定的（如 224x224x3），且像素值分布相对稳定，BN 可以利用 Batch 内海量的像素统计信息来加速收敛。但 NLP 的语义是离散且稀疏的，词向量维度的数值波动极大，BN 的“批间统计”根本压不住这种波动，只有 LN 的“个体内统计”才能稳住梯度的方差。

第五步：升华到现代大模型趋势（加分项，展示前沿视野）
甚至现在的 LLaMA、GPT 系列不仅用 LN，还进化成了 RMSNorm（去掉了 LN 中的均值平移，只保留缩放）。这恰恰印证了 LN 的核心思想：在 NLP 的高维空间中，特征的相对大小（缩放）比绝对数值（中心化）更重要，而 BN 那种强行拉齐批次内样本数值的做法，在语义建模中显得过于粗暴。
```

#### Q27： Encoder-only、Decoder-only 和 Encoder-Decoder 的区别吗？

```markdown
第一步：一句话定调（瞬间建立框架感）
“Encoder-only、Decoder-only 和 Encoder-Decoder 的本质区别，取决于你想让模型看多宽（双向），还是只能往前看（单向），以及是否需要建立两个序列之间的映射关系。一句话总结：Encoder-only 擅长‘阅读理解’，Decoder-only 擅长‘续写生成’，Encoder-Decoder 擅长‘翻译/总结’。”

第二步：拆解核心差异（根本是“注意力掩码”）
这是最硬核的技术区分点，你一定要强调掩码（Mask）：
Encoder-only（如 BERT）：双向（Bi-directional）自注意力。没有掩码，每个词都能看到句子中所有的词（左边和右边）。这赋予了模型最强的上下文理解能力。
Decoder-only（如 GPT）：单向（Causal / Auto-regressive）自注意力。带有因果掩码（Causal Mask），每个词只能看到它左侧（过去）的词，看不到右侧（未来）的词。这是为了做自回归生成（根据前文预测下一个词）。
Encoder-Decoder（如原始 Transformer、T5）：混合架构。编码器部分是双向的（看全源文本），解码器部分是单向的（自回归生成目标文本），并且在编码器和解码器之间插入了一个交叉注意力（Cross-Attention）层，让解码器每一步都去编码器里检索最相关的信息。
```

#### Q28：Feed Forward是什么

```markdown
第一步：一句话定调（极简定义）
“前馈网络（FFN）是 Transformer 中独立作用于每个位置的、简单的两层全连接神经网络。它承担了模型绝大多数的非线性变换和特征挖掘工作，是 Transformer 的‘知识存储和抽象思考’核心。”

第二步：精确的数学形式与维度变化（展现硬核基本功）
标准的 FFN 包含两个线性变换和一个非线性激活函数（原论文用 ReLU）：
升维（第一层）：输入维度先投影到高维空间 
激活（非线性）：引入 ReLU（或现在的 GELU/SwiGLU），让模型具备拟合复杂函数的能力。
降维（第二层）：投影回输入维度，保持残差连接和后续层输入的维度一致。

第三步：核心哲学——FFN 和 Attention 的“分工”（面试官最爱听的高度概括）
Attention 是“词与词之间的信息交融（特征混合）”，而 FFN 是“词自身的深层思考（特征变换）”。
	位置独立性（Position-wise）：FFN 在处理每个词的权重是完全共享的，且处理不同位置时互不干扰。这意味着无论句子多长，FFN 只是机械地对每个词做相同的数学运算，它不关心词的位置（位置信息全靠 Attention 那里的残差带过来）。
	参数大户：在 Transformer 中，FFN 占据了模型总参数量的约 2/3（远大于 Attention 的参数量）。因此，业界常说：“大模型的知识和事实记忆，主要存储在 FFN 的权重里。”——这句话极具技术洞察力，面试官听到这里眼睛会亮。
```

#### Q29：为什么 LoRA 微调主要调 Attention 不调 FFN？

```markdown
优先只调 Attention 的性价比远高于调 FFN
1. 低秩假设的“天然适配性”（数学结构死穴）
这是最硬核的理由，面试官听到绝对内心狂喜。
Attention 的权重矩阵（Wq,Wk,Wv,Wo）：本质上是计算Token 间的关联分数（相似度）。现实语义中，词与词的关系模式（如主谓宾、指代）往往是有限的、低秩的。用低秩矩阵（A×B）去逼近这种“关联模式”，误差极小。
FFN 的权重矩阵：FFN 是先升维（4倍）再降维，内部存储着海量的世界知识（事实、逻辑推理）。这种知识是高度复杂且非结构化的，本质上是高秩（High-rank）的。如果用极小的秩（比如 r=8）去强行逼近 FFN，相当于“用一根细线去拉一艘航母”，信息瓶颈极大，根本学不动。

2. 功能分工的“化学效应”（语义本质）
Attention：决定“在哪里找信息”（上下文路由 / 信息检索）。微调下游任务（如客服、角色扮演），主要改变的是模型“关注谁”的注意力模式。用LoRA微调 Attention，相当于“微调大脑的注意力开关”，极为灵敏，四两拨千斤。
FFN：决定“这些信息是什么”（知识存储 / 特征变换）。它存储的是预训练阶段积累的固化常识。如果强行微调 FFN，会过度扰动预训练的通用知识，极易引发灾难性遗忘（Catastrophic Forgetting）——模型可能学会新任务，但连“1+1=2”这种基础事实都记不清了。
```

#### Q30：那什么时候才会调 FFN？

```markdown
虽然优先调 Attention，但在以下两种场景，调 FFN 是必要的：

	持续学习（Continual Learning）注入新知识：当你要让模型学会 2026 年发生的新鲜事（比如新总统、新科技名词）时，FFN 必须被更新，因为这类事实性知识只存储在 FFN 里。

	使用 MoE（混合专家）架构：如果基座模型是 Mixtral 这类 MoE 模型，FFN 本身就是‘专家路由’的核心，不调 FFN 就无法改变任务分配路由。

但即便如此，针对 FFN 的 LoRA 秩（Rank）往往要设得比 Attention 大很多（比如 Attention 用 r=8，FFN 可能要用 r=64 或 128），才能勉强容纳信息的复杂度。”
```

#### Q30：为什么 Q 和 V 比 K 和 O 更重要？

```markdown
因为原论文实验表明，Wq 控制“查询路由”，Wv 控制“信息提取”，两者搭配能最有效地改变下游任务的判别边界。而 Wk（键映射）相对稳定，Wo（输出投影）主要做维度还原，改变它们收益极小。所以主流 Lora 默认配置都是 target_modules=["q_proj", "v_proj"]。
```

#### Q30：为什么LLM只保留Decoder？

```
LLM只保留Decoder（即Decoder-Only架构），并不是因为编码器（Encoder）没用，而是因为在‘通用人工智能’的范式下，编码器成了‘冗余的奢侈品’。 现代LLM选择砍掉Encoder，本质上是用‘算力’换‘通用性’，用‘深度’换‘复杂性’的一次极致简化。
```

#### Q31：MOE模型如何实现路由激活的？

```
https://aistudio.google.com/app/prompts/1Mw2FvnSP1KHKB492EY--t2fFLNcM4Fij
```



------



### 注意力变体

#### Q1：你为什么一开始没有直接用 MQA 或 GQA，而是从 MHA 出发？

```markdown
因为初期模型是基于通用基座（类似 LLaMA 架构），默认就是 MHA，且效果稳定。我们先做 baseline，再逐步做结构替换对比。
```

#### Q2：那你怎么判断 MHA 成为瓶颈的？

```markdown
主要是 KV cache 占用线性增长，batch 稍大就 OOM，同时 decode 阶段带宽瓶颈明显。
```

#### Q3：有没有量化指标？

```markdown
在 8K context 下，KV cache 占用约占显存 60%+，decode latency 占总推理时间约 70%。
```

#### Q4：MQA 和 GQA 的核心区别是什么？为什么工业界更偏 GQA？

```markdown
MQA 是所有 query head 共享一组 KV heads；GQA 是分组共享，比如 8 query heads 对 2 KV heads。

GQA 是 MHA 和 MQA 的折中方案，能兼顾效果和推理效率。
```

#### Q5：为什么 MQA 压缩更狠但不一定更好？

```markdown
因为 KV 信息被过度共享，会导致 attention 表达能力下降，尤其在复杂语义对齐任务上。
```

#### Q6：那 GQA 的“分组数”怎么选？

```markdown
一般根据 Q/K head ratio 和经验设置，比如 32Q / 8KV 或 32Q / 4KV。
```

#### Q7：GQA 在 KV cache 上具体优化了什么？

```markdown
主要减少 KV cache replication，把多个 query head 映射到共享 KV group，从而减少 key/value 存储数量。
```

#### Q8：MLA（Multi-head Latent Attention）你是怎么理解的？

```markdown
MLA 的核心是把 KV cache 从“显式 token 维度”压缩到“latent 向量空间”，通过低秩投影减少 KV 存储。
```

#### Q9：它和 GQA 本质区别是什么？

```markdown
GQA 是“减少 head 数”，MLA 是“压缩 KV 表示空间”。
```

#### Q10：MLA 为什么在长上下文特别有效？

```markdown
因为 KV cache 是 O(n) 增长，MLA 把 KV 压缩到 O(r)，r << n。
```

#### Q11：从 MHA → GQA → MLA 的演进逻辑是什么？

```markdown
核心是逐步压缩 KV 表示：
    MHA：表达最强，最耗显存
    GQA：减少 KV heads
    MLA：压缩 KV latent space
```

#### Q12：

```markdown

```

#### Q13：

